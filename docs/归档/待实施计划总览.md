# 待实施计划总览（2026-08 汇总 · ⚠️ 已降级为历史快照）

> 🔴 **时效警告（2026-09-10）**：本表是 **2026-08-26 的状态快照**，**已停止维护**。
> **完整、最新的方案文档索引与待办状态，一律以 [[TODO文件]] 为准**：
> - **方案文档全量索引** → [[TODO文件]] 顶部「📚 方案文档索引」
> - **待办状态** → [[TODO文件]] 正文各条目 + [[TODO已完成]]
>
> 保留本文件仅作"当时是怎么排优先级的"这一历史参考，**不要据此判断当前状态**。

> **创建日期**：2026-08-26
> **用途**：将分散在 TODO 文件、各方案文档中的"待实施计划"归纳为一张总表，
> 方便按优先级、按空闲时间择日执行。
> **原则**：所有计划均已评估定稿，**未执行**；实施前需用户确认 + 备份。
> **⚠️ 时效说明（2026-09-07，保留原文）**：下表 #1/#2/#7 已随"TODO 第一批"执行完毕（见 `docs/评估报告/TODO文件.md` 已完成表）；
> 本表保留作为执行快照参考，最新状态以 TODO 文件为准。剩余待办：#3/#4（Python 模拟数据 + AI 并发，**2026-09-09 已收口为 TODO #48**）、
> #5（秒杀管理）、#6（连接池）、#8（图片管理界面）。

**📌 2026-09-10 现状核对**（上文 8 项快照的当前状态，避免读者跨文件来回对照）：

| 快照项 | 现状 | 对应条目 |
|---|---|---|
| #1 服务器内存安全网（mem_limit + Swap 2G） | ✅ 已完成（2026-09-07，第一批） | R7 |
| #2 Nacos 堆 1g→512m + Sentinel -Xmx256m | ✅ 已完成（2026-09-07，第一批） | R7 |
| #3 Python 模拟数据生成（80/15/5 漏斗） | 🟡 已收口，方案定稿待做 | **#48** |
| #4 AI 并发测试（本地 mock LLM） | 🟡 同上（同一方案文档） | **#48** |
| #5 秒杀管理功能（后台 UI + 场次维度标记） | 🟡 未做 | **#3**（中优先级） |
| #6 连接池统一 HikariCP（6 服务 Druid→Hikari） | 🟡 未做（需全量回归窗口） | **#R5** |
| #7 Redis 加固 + 哨兵模式 | ✅ 加固 2026-09-07（R1~R4）、哨兵 2026-09-09 | R1~R4 + **#9** |
| #8 商品图片 / 品牌 Logo 管理界面 | 🟡 未做 | **#1**（中优先级） |

---

## 一、总览表（按优先级排序）

| # | 计划 | 状态 | 预计耗时 | 风险 | 详细方案 |
|---|------|------|---------|------|---------|
| 1 | **服务器内存"安全网"**（mem_limit + Swap 2G） | ✅ 已执行（2026-09-07，R7） | ~30 分钟 | 🟢 低 | `docs/归档/服务器内存优化方案.md` |
| 2 | **Nacos 堆 1g→512m + Sentinel -Xmx256m** | ✅ 已执行（2026-09-07，R7） | ~15 分钟 | 🟡 中 | `docs/归档/服务器内存优化方案.md` |
| 3 | **Python 模拟数据生成**（80/15/5 行为分布） | 方案定稿 | 写脚本 ~2h | 🟢 低（可清理） | `docs/评估报告/Python模拟数据与AI并发测试方案.md` |
| 4 | **AI 并发测试**（本地 mock LLM，不调真实 API） | 方案定稿 | ~1h | 🟡 中（需先做 #1 内存安全网） | 同上 |
| 5 | **秒杀管理功能**（后台 UI + 场次维度购买标记） | 方案定稿 | 大（2-3 天） | 🟡 中（改标记维度需迁移数据） | `docs/评估报告/TODO文件.md` 中优先级 #3 |
| 6 | **连接池统一 HikariCP**（6 服务从 Druid 切换） | 方案定稿 | 大（全量回归+重启） | 🔴 中高 | `docs/评估报告/连接池统一HikariCP方案.md` |
| 7 | Redis 加固（密码/AOF/mem_limit） | ✅ 加固部分已执行（2026-09-07，R1~R4）；哨兵模式未做 | ~1h | 🟡 中 | `docs/归档/Redis配置加固与哨兵模式方案.md` |
| 8 | 商品图片/品牌Logo 管理界面（后台前端） | 未启动 | 中 | 🟢 低 | `docs/评估报告/TODO文件.md` 中优先级 #1 |

---

## 二、推荐执行顺序（按"收益/风险"排序）

### 第一批：运维安全（30 分钟，零代码）
> **收益**：防 OOM 杀服务（历史杀过 ES）+ 省 ~600M 内存
> **前置**：无，独立可做

1. **mem_limit 全容器**（`docker update --memory` 零重启即时生效）
2. **Swap 2G**（需 ecs-user sudo，`fallocate` + `swapon`）
3. **Nacos 降堆 512m**（省 ~480M，重启 nacos 2-5 分钟抖动）
4. **Sentinel -Xmx256m**（省 ~100M）

> 做完后 `free -h` 可用内存 2.1G → ~2.7G，且从此有硬边界 + 缓冲垫。
> 📄 详见 `docs/归档/服务器内存优化方案.md`

### 第二批：数据与压测（半天）
> **前置**：第一批完成（AI 并发需要内存余量）

5. **写 Python 模拟数据脚本**（80/15/5 漏斗 + `test_sim_` 前缀可清理）
6. **本地 mock LLM + AI 并发测试**（20 → 50 → 100 逐步加压，盯 `free -h`）
7. **慢节奏跑 1-2 天模拟数据** → 生成"运营 30 天"的面试素材

> 📄 详见 `docs/评估报告/Python模拟数据与AI并发测试方案.md`

### 第三批：业务功能（大项，有空再做）

8. **秒杀管理后台**（场次 CRUD + 时间窗口 + 库存管理）
9. **秒杀购买标记改为场次维度**（支持"每轮可买一次"，需数据迁移评估）
10. **连接池统一 HikariCP**（6 服务切换，需全量回归）
11. **Redis 加固 + 哨兵**（密码/AOF/内存上限）

---

## 三、各计划速查卡

### ① 内存安全网（第一批，最推荐先做）
```bash
# mem_limit（零重启即时生效）
docker update --memory 1g csmall-product csmall-order csmall-seckill csmall-front csmall-ai
docker update --memory 768m csmall-sso csmall-ums csmall-ams csmall-gateway csmall-search csmall-resource
docker update --memory 1.5g csmall-es csmall-skywalking-oap
docker update --memory 1g csmall-nacos csmall-mysql
docker update --memory 512m csmall-sentinel csmall-seata csmall-skywalking-ui
docker update --memory 256m csmall-rabbitmq csmall-redis csmall-frontend

# Swap 2G（需 ecs-user）
sudo fallocate -l 2G /swapfile && sudo chmod 600 /swapfile
sudo mkswap /swapfile && sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

### ② Python 模拟数据（第二批）
- 行为分布：浏览 80% / 加购 15% / 下单+支付 5% / 秒杀 0.5%
- 用户前缀 `test_sim_`，提供 `--clean` 一键清理
- 秒杀窗口当前全年有效，跨年后需先更新
- 同一用户同一 SKU 支付后永久不能复购（reseckill 永久标记）→ 脚本换 SKU 或换用户

### ③ AI 并发测试（第二批）
- **不调真实 API**（TokenBudget 2 元/天，压测会打满）
- 本地 mock LLM（`base-url` 指向本地 9999 端口）→ 测的是服务端承载（Tomcat 线程池/SSE/Redis），不是模型速度
- 从 20 并发起步，逐步加到 100，盯 `docker stats csmall-ai` + `free -h`

### ④ 秒杀管理（第三批）
- 现状：无管理 UI，全靠手动改库；购买标记按 `skuId:userId` 永久 → 无法"每轮复购"
- 改造：管理后台 + 场次维度标记 + success 索引调整
- ⚠️ 改维度影响旧数据，需迁移评估

### ⑤ 连接池统一（第三批）
- 6 服务从 Druid → HikariCP（仅 resource 已是 HikariCP）
- 陷阱：`mall-ums-service`/`mall-product-service` 的 pom 排除了 HikariCP，切换前必须移除
- 需要全量回归 + 重启窗口

---

## 四、执行前 Checklist

- [ ] 每项实施前：`cp /data/csmall/docker-compose.yml docker-compose.yml.bak.$(date +%Y%m%d)`
- [ ] 服务器操作以 ecs-user 登录（sudo 需要），<AI账号> 只能 docker 操作
- [ ] 大项（秒杀管理/连接池）改代码后本地先验证，再部署
- [ ] 模拟数据演示前必须 `--clean` 清理（保持压测数据干净）

---

**关联文档**：`docs/评估报告/TODO文件.md`（完整条目）、`docs/归档/服务器内存优化方案.md`、
`docs/评估报告/JVM调优方案.md`（已完成的第一轮）、`docs/评估报告/Python模拟数据与AI并发测试方案.md`、
`docs/评估报告/连接池统一HikariCP方案.md`、`docs/归档/Redis配置加固与哨兵模式方案.md`
