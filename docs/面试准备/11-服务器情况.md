# 11 服务器情况

> **核心问题**：服务器资源、部署架构、网络、安全
> 难度：低-中（基础但常被问）
> **数据快照**: 2026-09-04 服务器实测 + **2026-09-12 复核更新**（⚠️ **环境已从单机变为两台机器**；R7 内存优化 / #24 MySQL 强密码 / #25 JWT 随机化 / #13 Nacos 认证 / #29 数据库备份 / #9 Redis 主从哨兵 / #4 秒杀双实例 均已完成，见 Q1/Q2/Q3/Q5/Q6/Q8/Q10；内存为动态值，面试前按最新状态核对）

> **内容时效声明（2026-09-06 第一轮复习完成）**：本文档内容已按当时服务器/代码实测核对。**此后不随意修改**——若因 TODO 实施、代码变更导致内容过期，先更新对应事实来源文档（问题解决/评估报告/上下文文档），再回来改本档；面试使用请以最新实测为准。
> 📌 **2026-09-12 复核更新（按上述流程回填）**：第一批（R7/#24/#25）、第二批（#13/#29/#33 等）、第三批（#9 跨机 Redis 主从哨兵 / #4 秒杀双实例）**均已执行完毕**，本文档相应章节已从「方案/待执行」改为「已完成 + 实测值」。**新增的事实基线：项目自 2026-09-09 起是两台机器**（老机 21 容器 + 新机 5 容器，compose 为两机合并的单一文件、共 26 服务）。

---

### Q1. 服务器配置？🔴

【追问1】多少核多少内存？
【追问2】什么系统？

【简答】
**两台**阿里云 ECS（同 VPC、同安全组，内网互通实测 0.43ms）：
- **老机**（主环境）：4核16G，Ubuntu 24.04，成都地域，公网 IP 8.156.77.197 / 私网 172.29.193.239 —— 跑 **21 个容器**（全栈）
- **新机 `csmall-node2`**（2026-09-09 创建，跨机集群实验）：2核8G，公网 IP 47.109.70.197 / 私网 172.29.193.240 —— 跑 **5 个容器**（Redis 从库 + 3 哨兵 + 秒杀副本）

【深挖】
- **老机（主环境）**：ecs.e-c1m4.xlarge（经济型），4 vCPU / 16 GiB，100G 系统盘（实测 99G，已用 38G）；**5 Mbps 固定带宽**；Ubuntu 24.04.4 LTS；西南1（成都）
- **新机（跨机集群）**：ecs.e-c1m4.large（经济型），2 vCPU / 8 GiB，40G 系统盘；**按使用流量计费**（峰值 5 Mbps，跨机流量走内网、公网只跑 SSH）；同地域同可用区
- **两台关系**：同 VPC（`172.29.193.0/20`）+ **同一安全组** ⇒ 内网互信、不限速、不计费；`deploy/docker/docker-compose.yml` 是**两机合并的单一文件**（**26 个服务** = 老机 21 + 新机 5）⇒ ⚠️ 新机上**永远不要裸跑 `docker compose up -d`**（会把老机 21 个服务一起拉起来），必须带服务名
- **面试话术**："**两台机器**：老机 4核16G 跑 21 个容器（全栈主环境），新机 2核8G 跑 5 个容器（Redis 从库 + 3 哨兵 + 秒杀副本）。老机内存是主要瓶颈（所以做了 JVM 调优），5M 带宽所以压测打内部端口；跨机流量走内网私网地址，不占公网带宽，也不走按量计费"

---

### Q2. 21 个容器怎么分布的？资源怎么分配的？

【追问1】哪个最吃内存？
【追问2】为什么这么吃？
【追问3】新机那 5 个容器是干什么的？

【简答】
**老机 21 容器** = 11 微服务 + 9 中间件（含 SkyWalking OAP/UI）+ 1 前端。最吃内存的是 Nacos、mall-product、mall-seckill 等 JVM 服务（实时 RSS ~800M~1G 级别），Seata 已调优（~0.38G）。
⚠️ **「21 容器」是老机在跑的服务数**；compose 文件共 **26 个服务**，另 **5 个在新机**（`csmall-redis-replica` 6380 + `csmall-sentinel-1/2/3` 26379-26381 + `csmall-seckill-2` 10017）。

【深挖】
- **老机分布（21）**：11 微服务（gateway/sso/product/front/order/search/seckill/ums/ams/resource/ai）+ 9 中间件（MySQL/Redis/Nacos/RabbitMQ/ES/Seata/Sentinel/SkyWalking OAP/UI）+ 1 前端 Nginx
- **新机分布（5，跨机集群成员）**：`redis-replica`（Redis 从库，`replicaof 172.29.193.239 6379`）+ `redis-sentinel-1/2/3`（哨兵，quorum=2）+ `mall-seckill-2`（秒杀第二实例）——新机同时兼作**造数/压测执行位**（压测必须在被测系统之外跑，不能自压自伤）

**内存排行（⚠️ 2026-09-04 docker stats 快照 = R7 执行前；R7 已于 2026-09-07 执行，见下）**：

| 容器 | RSS | 说明 |
|------|-----|------|
| csmall-nacos | ~976M | JVM 默认堆偏大 → **R7 已把它降到 `JVM_XMX=512m`（2026-09-07 执行，实测容器 env 确认）** |
| csmall-product | ~950M | JVM 服务：堆 + 堆外 + SW agent |
| csmall-seckill / order | ~810M / ~790M | 同上（-Xmx448m 但 RSS 含堆外） |
| csmall-front / ams / ums / ai | ~690M / ~660M / ~650M / ~580M | JVM 服务 |
| csmall-mysql | ~510M | DB buffer pool |
| csmall-seata | ~386M | **已调优**（JVM_XMX=512m，符合 08-04 调优记录） |

- **为什么吃**：11 个 JVM（堆 + 堆外 + SW agent 插件类）+ ES/MySQL 等中间件各自开销叠加；Nacos 未降堆曾是最大可省项 —— **R7 已于 2026-09-07 执行**：Nacos 堆 `1G→512m` + Sentinel 加 `-Xmx256m` + **21 容器全部加 `mem_limit`**（实测 `docker inspect` 抽样均为 1 GiB）+ **Swap 2G**
- **面试话术**："内存大头是 JVM 服务（11 个）+ 中间件。第一轮调优（08-04）把 Seata 从 1.46G 压到 0.38G；第二轮 **R7（2026-09-07）**又做了 Nacos 降堆、全容器 `mem_limit` 安全网、Swap 2G 缓冲——**调优是持续过程，不是一次到位**"

> ⚠️ 历史口径（面试若被问"调优前后对比"用）：08-04 调优前 Seata 1.46G > OAP 1.17G > ES 1.12G > Nacos 963M；调优后 Seata 0.38G、OAP 1.08G，整体 13G→10G。**此后运行态增长又涨回，当前 ~12G used（见 Q5）。**

---

### Q3. 网络是怎么设计的？哪些端口对外？🔴

【追问1】安全组？
【追问2】中间件端口为什么不暴露？

【简答】
安全组只开放 22（SSH）、80（HTTP）。数据库/Redis/Nacos 等中间件端口只在 VPC 内网，不对公网。**两台机器共用同一安全组** ⇒ 同组内网互信（跨机集群走内网私网地址）。

【深挖】
- **对外**：22（SSH）、80（前端 HTTP + API 反代）
- **内网**：3306（MySQL）、6379（Redis）、8848（Nacos）、9200（ES）等都不暴露公网
- **两机同安全组**：老机与新机共用 `sg-2vc9i0zxt8gbq18mo8ea` ⇒ **VPC 内网（172.29.193.0/20）互通、不限速、不计费**；Redis 主从/哨兵、秒杀副本的跨机通信全走内网私网地址
- **3306/6379 的绑定形态**：2026-09-09 跨机集群改造时，从 `127.0.0.1` 改为绑**宿主私网 IP** `172.29.193.239`（**不是 `0.0.0.0`**，作纵深防御）
- ⚠️ **边界认知（面试加分）**：阿里云公网 IP 是 **NAT 映射、不在网卡上** ⇒ 服务绑 `0.0.0.0` 还是绑私网 IP，**公网可达性完全等价**（实测 `6379/3306/10087/26379/6380` 公网全 blocked，仅 `:80` 通）——**安全组才是唯一的公网边界**，`bind`/`ports` 只决定"本地哪些地址接受连接"，属**纵深防御**；分层模型 = **安全组（边界）→ bind（监听面）→ 认证（身份）→ TLS（传输）**
- **为什么**：中间件暴露公网 = 被攻击面。SSH 隧道访问管理界面（Nacos/Sentinel/SkyWalking）
- **访问方式**：`ssh -L 8090:localhost:8090 ecs-user@IP` 本地隧道访问内网管理界面
- **面试话术**："**最小暴露面**——公网只开 22/80，中间件全走内网，管理界面用 SSH 隧道访问。顺带一个我很在意的认知：阿里云公网 IP 是 NAT 映射、**不在网卡上**，所以服务绑 `0.0.0.0` 和绑私网 IP 的**公网可达性一模一样**——我实测过 6379/3306/10087 公网全是 blocked。真正挡住攻击的是**安全组**，bind 只是纵深防御的一层，不能拿它当边界"

---

### Q4. 服务器目录结构？数据存哪？

【追问1】/data 下有什么？
【追问2】图片存哪？

【简答】
`/data/csmall/`（compose、jars、前端、**backup**）、`/data/csmall-upload/`（图片）。数据卷由 Docker 管理。**两台机器各有 `/data/csmall/`**（跑同一份 compose 文件）。

【深挖】
- **/data/csmall**：docker-compose.yml（**两机合并的单一文件**）、.env、jars/（11 个 JAR）、frontend/（dist）、dockerfiles/、**backup/**（每日 02:30 的 mysqldump 落这里，见 Q8）
- **/data/csmall-upload**：用户上传图片，Nginx 直接静态服务
- **数据卷**：`/var/lib/docker/volumes/`（`mysql_data`、`redis_data`、`es_data`、`rabbitmq_data`、**`nacos_data`**（#46 挂载，重启不丢 Nacos 配置）等），容器重建不丢
- **⚠️ 易踩坑**：新机在跑的是**同一份 compose 文件**里的另外 5 个服务 ⇒ 在新机上**永远不要裸跑 `docker compose up -d`**（会把老机 21 个服务一起拉起来），必须带服务名
- **面试话术**："项目文件和数据分离——**代码在 /data/csmall，图片在 upload，数据库在 Docker 卷**，各有各的持久化策略。另外我把 Nacos 的配置也挂了数据卷：踩过一次坑（重建容器后配置丢失），所以数据卷这层不能省"

---

### Q5. 现在服务器内存使用率？为什么？🔴

【追问1】调优后多少？
【追问2】还紧张吗？

【简答】
**2026-09-04 快照：used 12G/14G（~86%），available 仅 2.1G，无 Swap**——当时内存紧张（曾于 2026-08-04 调优到 71%，运行态增长后又涨回）。
✅ **2026-09-07 已执行 R7 内存优化**（Nacos 降堆 + 全容器 mem_limit + **Swap 2G**）；**2026-09-12 复核实测：available ~3.1G、Swap 2G 已激活**。

【深挖】
- **08-04 第一轮调优后**：13G/14G（93%）→ 10G/14G（71%），可用 1.1G → 4.4G（当时快照；16G 硬件实际可用约 14G，百分比按 14G 基数）
- **09-04 快照**：used 12G / 14G（~86%），free 270M，available 2.1G，Swap 0 → 回到紧张区
- **为什么反弹**：调优释放的空间被**各服务运行态增长**吃回（SkyWalking OAP 调优时已存在，非新增；主要是 11 个 JVM 的堆外/缓存随运行时间增长）+ Nacos（当时未降堆）仍吃 ~976M
- ✅ **已执行：R7 内存优化（2026-09-07）**——① **Nacos 堆 `1G→512m`**（`JVM_XMS/JVM_XMX=512m`，实测容器 env 确认）+ Sentinel 加 `-Xmx256m`；② **21 个容器全部加 `mem_limit`**（实测 `docker inspect` 抽样 = 1 GiB，安全网）；③ **Swap 2G 已激活**（`/swapfile` 2G + `/etc/fstab` 已写入 ⇒ 重启仍生效）。**执行后实测（2026-09-12）：available ~3.1G**
- **面试话术**（诚实版，改成已完成 + 有实测值）："我做过两轮内存治理：第一轮 JVM 调优（93%→71%，释放 3G），但**调优是动态过程**——加了 SkyWalking 后又回到 86%。所以我出了 R7 方案并**已执行**：Nacos 降堆到 512M、21 个容器全加 `mem_limit` 当安全网、加 2G Swap 缓冲，实测 available 从 2.1G 回到 ~3.1G。**我不会把'曾经调好'说成'一直很好'**——能讲清每轮的收益边界和下一步（比如 ES/MySQL 的内存我没动，因为收益小、风险大）才是真理解"

---

### Q6. 如果服务器内存又不够了，你怎么处理？

【追问1】临时怎么办？
【追问2】长期怎么办？

【简答】
临时：清理日志/缓存、重启释放。长期：JVM 再调优、合并服务、或升级服务器。

【深挖】
- **临时**：`docker logs` 清理、ES 索引清理、重启服务释放堆
- **长期**：继续 JVM 参数优化（堆外限制）、无状态服务合并、或换更大配置
- ✅ **已落地**：TODO R7（`mem_limit` + Nacos 降堆 + Swap 2G）**已于 2026-09-07 执行**（见 Q5），是所有容器的一道"安全网"——单个进程失控最多吃满自己的 limit，不会拖垮宿主
- **下一步（还没做，讲认知）**：ES 堆 / MySQL buffer pool 我没动（收益小、影响性能）；真实扩容路径是**拆分机器**——2026-09-09 已经这么做了：把 Redis 从库 + 哨兵 + 秒杀副本迁到第二台机器（新机 2C8G）
- **面试话术**："内存管理是**动态过程**——先临时清理救急，再长期调优，实在不够就加资源。我按这个思路做了两轮：第二轮 R7 **已经执行完**（Nacos 降堆 + 全容器 `mem_limit` + Swap 2G，available 2.1G→~3.1G）；而且我不止压内存，还把**能从老机挪走的组件真的挪走了**——Redis 从库、3 个哨兵、秒杀第二实例都跑在第二台机器上"

---

### Q7. 安全组只开 80/22，那 AI 模块的 API 调用怎么办？

【追问1】出站流量？
【追问2】DeepSeek 调用？
【追问3】入站出站具体怎么验证的？（实测背书）
【追问4】服务器上跑 AI 代码助手，安全上怎么隔离的？（误报插曲）

【简答】
安全组限制的是**入站**（别人访问我），出站（我访问 DeepSeek API）不受限。所以服务器可以正常调 DeepSeek/硅基流动 API。

【深挖】
- **入站 vs 出站**：安全组是"有方向的防火墙"——入方向和出方向是**两套独立规则**；新建安全组默认**出方向全放行**（系统自动加"出方向允许所有流量"规则），只对入方向收紧 → 你配的"只开 22/80"只挡"别人访问你"，不挡"你访问别人"
- **官方依据**：阿里云安全组规则文档明确分入/出方向、默认出方向放行
- **AI 调用**：服务器出站调 DeepSeek API、Embedding API，不受 22/80 限制影响

**🧪 实测背书（2026-09-04，宿主机 + 容器内双向验证）**：

| 实测 | 结果 | 解读 |
|------|------|------|
| 宿主机 → api.deepseek.com | HTTP 401（TLS 0.015s） | ✅ 网络通；401=没带 key，只差认证 |
| 宿主机 → api.siliconflow.cn | HTTP 404 | ✅ 网络通；404=根路径无资源 |
| **csmall-ai 容器内** → DeepSeek | HTTP 401 | ✅ 容器内也通（真实调用路径是容器，不是宿主机） |
| 外部连公网 IP:3306 | 超时/拒绝 | ✅ 入站确实被安全组挡住 |

- **🛡️ 小插曲：AI 运维触发的安全告警（误报 + 处置 = 面试好素材）**：
  - 事件：AI 代码助手（<AI账号> 账号）远程执行网络测试时，用了 `echo <base64> | base64 -d | sh` 的编码脚本形态 → **阿里云云安全中心告警"可疑编码命令"**（检测模型把"base64 编码 + 管道给 sh"识别为木马/后门常见混淆手法）
  - 处置流程（正确姿势）：收到告警**不慌 → 先验证再处置**——解码告警中的 base64 → 内容 = 自己刚跑的 curl 连通性测试（只读、无破坏）→ 确认为**误报** → 控制台标记误报/加白 + 今后改用直连命令避免编码形态
  - 为什么有价值：① 证明**服务器上有行为检测且真的在抓可疑特征**（监控不是摆设）② AI 账号是**独立隔离账号**（<AI账号> = docker 组、无 sudo、专属密钥，见 [[服务器权限收紧方案]]：sudoers 白名单 + 每模型独立账号可单独吊销）→ 就算账号真被攻破，破坏面也受限 ③ 展示"告警处置方法论"：先验证 IOC 准确性，再决定忽略还是处置（阿里云官方处置建议也是这个流程）
- **面试话术**："安全组是**入站防火墙**——限制别人访问我，不影响我主动访问外部 API（DeepSeek/Embedding），我用 curl 实测过：宿主机和 AI 容器里访问 DeepSeek 都通（401=只差认证），外部连我 3306 是超时的，入站出站两个方向用实验验证过。还有个有意思的小插曲：我的 AI 助手在服务器上跑脚本触发过一次'可疑编码命令'告警——解码一看是我自己的测试脚本，误报。但这件事让我理解了三点：**云厂商的行为检测是真的在抓东西、AI 运维账号要按模型隔离（docker 组无 sudo、可单独吊销）、收到告警先验证再处置**。我把 AI 助手的权限收敛和密钥管理都写成了文档（[[服务器权限收紧方案]]）"

---

### Q8. 数据备份做了吗？

【追问1】数据库备份？
【追问2】代码备份？

【简答】
代码在 GitHub（天然备份）；数据库 ✅ **已完成自动化备份**：`backup-db.sh` + cron **每日 02:30 全量 dump** + 保留策略（#29，2026-09-08），并且**做过恢复演练**（#47，独立临时容器验证）。

【深挖】
- **代码**：GitHub 仓库（csmall + csmall-vue），天然版本管理 + 备份 ✅

**数据库备份现状（2026-09-12 实测）**：

| 项 | 实测结果 | 结论 |
|----|---------|------|
| Docker 卷持久化 | mysql/redis/es/rabbitmq 4 卷都在 | ✅ 容器重建不丢 |
| 备份脚本 | `/data/csmall/backup/backup-db.sh`（2026-09-08 落地） | ✅ |
| cron 定时任务 | 每日 **02:30**（实测备份文件名连续：`_20260909_0230` / `_0910_0230` / `_0911_0230` / `_0912_0230`） | ✅ |
| 备份产物 | `/data/csmall/backup/*.sql.gz`（6 库全量，实测约 49~50 KB/份，`gzip -t` 校验通过） | ✅ |
| 保留策略 | 7 天循环覆盖（+ 造数前的手工快照） | ✅ |
| 恢复演练 | ✅ **已做**（用独立临时容器还原验证，#47） | ✅ |

- **关键认知（仍然有效）**：**卷持久化 ≠ 备份**——卷在宿主机 `/var/lib/docker/volumes/`，防的是"容器重建丢数据"；**若整机磁盘损坏/误删卷，数据照样没**。所以必须另有 dump 落到独立文件
- **面试话术**："我最认同的一句话是『**没验证过的备份等于没有**』。所以我做了三步：① 每日 02:30 全量 dump 6 库 + 保留 7 天（先确认卷持久化只是底线、不是备份）；② 备份文件落 `/data/csmall/backup/` 并做非空校验；③ **用独立临时容器真做了一次恢复演练**——不是"我配了脚本所以应该有备份"，而是"我把它还原出来验证过"。造数/演练这类会改数据的操作，我还会在开跑前强制打一次快照并把文件名登记进批次记录，保证**可回滚**"

---

### Q9. 域名/HTTPS 配置了？

【追问1】为什么没配？
【追问2】怎么配？
【追问3】HTTP 和 HTTPS 的区别？TLS 握手？
【追问4】TCP 三次握手为什么是三次？和 TLS 握手的区别？

【简答】
没配 HTTPS，因为无域名 + ICP 备案要求服务器续费 3 个月以上（预算不足）。配置 HTTPS 需要：域名 → 备案 → SSL 证书（Let's Encrypt）→ Nginx 配 443。

【深挖】
- **HTTP vs HTTPS 核心**：HTTPS 不是新协议 = **HTTP 跑在 TLS 之上**（HTTP over TLS），端口 80→443。TLS 解决三个问题：

| 问题 | HTTP 的暴露 | HTTPS 解决 |
|------|------------|-----------|
| 机密性 | 明文传输，抓包看到密码/Token | 对称加密，内容不可读 |
| 完整性 | 中间人篡改内容 | MAC 校验，改了即发现 |
| 身份认证 | 任何人可冒充服务器 | CA 证书证明"我是真的" |

> 类比：HTTP = 寄明信片（谁都能看能改）；HTTPS = 上锁保险箱快递（带防拆封条）。

- **基础概念（被追问时的弹药，逐个讲清）**：

**RTT（往返时间）**：一个包"你 → 服务器 → 应答回你"的总时间。HTTPS 比 HTTP 慢的原因不是计算慢，而是握手多等了几次往返（RTT）。

**对称加密**：加密和解密用**同一把钥匙**（快，适合加密大量数据）。类比：一把钥匙开一把锁，锁上再开锁都靠它。

**非对称加密**：**两把钥匙**——公钥（公开，谁都能拿）+ 私钥（私藏，只有自己有）。两个用途，各配一个例子：

| 场景 | 做法 | 效果 |
|------|------|------|
| ① 发密信（保机密） | 别人用你的【公钥锁】把信锁进箱子寄给你 | 只有你的【私钥】能打开，路上谁拿到都看不了 |
| ② 验身份（防冒充） | 你用【私钥】给公告盖章 | 大家拿你的【公钥】验证，能验过 = 一定出自你 |

> 关键认知：非对称很慢（大数运算），所以 HTTPS 里它只干一件事——**安全传递"会话密钥"**，数据本身不靠它。

**HTTPS = 混合加密**（非对称传钥匙 + 对称传数据，各用所长）：

```
① 浏览器生成一个随机"会话密钥"
② 用服务器的【公钥】加密后发给服务器     ← 非对称（只此一次，慢但安全）
③ 服务器用【私钥】解开，拿到会话密钥
④ 之后所有 HTTP 数据用会话密钥加密传输    ← 对称（快，量大也不怕）
```

> 类比：非对称 = 保险柜运钥匙（慢但绝对安全）；对称 = 拿到钥匙后用普通锁进出（快）。HTTPS 先把钥匙安全运到，再用钥匙快速开锁。

**证书**：服务器的"电子身份证"——{域名 + 服务器公钥 + 颁发者 + 有效期 + CA 签名}。作用：**把"公钥"和"域名"绑死**，防止中间人塞给你一把假公钥。

**CA**：发证书的可信第三方（DigiCert / Let's Encrypt）。浏览器预装"可信 CA 名单"，验证证书四连问：① 发证 CA 在不在名单？② 域名匹配吗？③ 过期没？④ 用 CA 公钥验签真不真？——全过才信任。

- **TLS 握手（简化）**：① ClientHello（我来了+支持的加密套件）→ ② ServerHello + 证书 → ③ 客户端验证书（可信 CA？域名匹配？）→ ④ 用证书公钥加密"预主密钥"发出 → ⑤ 双方算出相同会话密钥 → 之后 HTTP 内容对称加密传输

- **TCP 三次握手 vs TLS 握手（两层、先后发生）**：

| | TCP 三次握手 | TLS 握手 |
|--|------------|---------|
| 层 | 传输层（建可靠连接） | TLS 层（协商加密，在 TCP 之上） |
| 目的 | 双方确认"能收能发" | 协商加密密钥 + 验证身份 |
| 开销 | 1.5 RTT | 1-2 RTT（可优化 1-RTT/0-RTT） |
| 顺序 | 先 TCP | 再 TLS，之后才是 HTTP |

- **为什么 TCP 握手要三次不是两次（幽灵请求，展开理解）**：
  - 完整时间线：客户端发 SYN#1 → **网络拥塞卡住** → 客户端超时重发 SYN#2 → SYN#2 正常建连、传完数据、关闭 ✅ → **此时迟到的 SYN#1 才到服务器**
  - 若只两次握手：服务器不知道 SYN#1 是过期幽灵 → 回 SYN+ACK → **认为连接建立、分配资源傻等客户端发数据** → 但客户端连接早用完了，不会理它 → **服务器资源白占**（攻击者狂发只 SYN 不应答 = SYN Flood 攻击，靠这个原理打垮服务器）
  - 三次握手：客户端收到幽灵的 SYN+ACK → "我没发过对应的新 SYN"（我的 SYN#2 早完成握手了）→ **不回应 ACK** → 服务器等不到 ACK → 知道没人要这个连接 → 主动释放 ✅
  - 本质：**两次握手只能确认"服务器能收能发"，确认不了"客户端能收"**——服务器区分不了"新请求 vs 迟到幽灵"；第三次 ACK 由客户端发出 = 告诉服务器"连接是活的，我确实在"，服务器才放心分配资源。类比：电话里对方说"喂?"（SYN），你回"喂?听到吗"（SYN+ACK），对方再说"听到了"（ACK）——三句才确认双方通话正常，只来回两句总有一方不确定
- **怎么配（若上 HTTPS）**：certbot 获取 Let's Encrypt 证书 → Nginx 443 + 80 跳转 → cron 自动续期
- **阻塞链**：HTTPS → SSL证书 → 域名 → ICP备案 → 服务器续费≥3月 → 预算不足
- **加分点（TLS 终止）**：证书只放 **Nginx（边缘）**，Nginx 终止 TLS → 内网反代 HTTP 到 Gateway → 微服务间容器网络走 HTTP 明文无妨——**只有边缘要加密**，不是每个服务都配证书
- **面试话术**："HTTPS 卡在**域名 + 备案**的流程上——国内服务器要备案，备案要求续费 3 个月。技术上我很清楚：HTTPS = HTTP over TLS，解决机密性/完整性/身份认证三件事；TLS 握手在 TCP 三次握手之后，用非对称安全交换会话密钥、之后对称加密传输。TCP 三次握手为什么是三次——为了防'迟到幽灵 SYN'让服务器白等，第三次 ACK 是客户端给服务器的'连接是活的'确认。真上 HTTPS 我会用 Let's Encrypt + Nginx 边缘终止 TLS，微服务内网保持 HTTP。"

---

### Q10. 如果项目要上生产（真实用户），你会改什么？🔴

【追问1】基础设施？
【追问2】架构？

【简答】
✅ **已经做掉一大半**：中间件 HA（Redis 主从+哨兵）、安全加固（密钥随机化/MySQL 强密码/Redis 密码/Nacos 认证）、数据库备份自动化、跨机双实例。
**还差的**：① Nacos/ES/MQ 仍单点（未集群）；② HTTPS + 域名（卡在备案/预算）；③ 主动告警（Prometheus）+ 日志聚合；④ 容器 restart 策略、镜像版本化/CI 等交付层。——每条我都落成了 TODO 方案，不是嘴上说说。

【深挖】
- **上生产的差距全景（每条对应 TODO #，**状态已按 2026-09-12 实测回填**）**：

| 领域 | 差距（一句话） | TODO #（状态） |
|------|--------------|---------------|
| 安全 | JWT 密钥 = 代码默认值，clone 仓库可伪造 token | ✅ **#25 已完成（2026-09-07）**：换 64 字符随机密钥 + 11 容器同步 + 全链路回归 |
| 安全 | Redis/MySQL 弱密码 + 端口全映射 0.0.0.0 | ✅ **#24 已完成（2026-09-07）**：MySQL **43 位强密码**（`ALTER USER` 双 host）、Redis 加 `requirepass`、3306/6379 收窄（跨机后绑私网 IP）；⚠️ 中间件容器仍 root 运行（未做） |
| 安全 | MQ 仍是默认 `guest/guest` | ✅ **#54 已完成（2026-09-12）**：切专用 `cs_mq_admin`(administrator)，`guest` 已删除 |
| 安全 | SSH 暴露面 | ✅ **#55 已评估·接受（2026-09-12）**：root **本就是锁定账号**（`passwd -S root` = `L`，无密码可爆破）；`ecs-user` 保留密码登录（有爆破噪音但密码强 + 学习用途）；副产品**密钥登录已打通** |
| 安全 | 无登录限流/密码策略/MFA（哈希只是第一层） | #17（P0 部分**未做**，面试讲认知） |
| 安全 | Nacos 无认证，内网失陷可注册假服务 | ✅ **#13 已完成（2026-09-08）**：`NACOS_AUTH_ENABLE` + 随机 TOKEN/IDENTITY + 11 服务客户端同步，实测无 token = **403** |
| 网络 | 单网段大锅烩，无应用网/数据网隔离 | #26（P2，暂缓——现为单个 bridge 网络） |
| 高可用 | Redis 单点 + 无哨兵（丢了秒杀直接停摆） | ✅ **#9 已完成（2026-09-09）**：**跨机** 1 主 1 从 + **3 哨兵**（实测选主 **6.1s** / 客户端 **9.1s** 自愈 / **数据零丢失**）+ #14 五层防护 + #14-P2 `min-replicas-to-write 1` |
| 高可用 | 微服务单副本，集群化未做 | ✅ **#4 已完成（2026-09-09）**：秒杀**跨机双实例**（老机 + 新机各一实例，负载实测 **60 请求 = 30:30**，停实例剔除通过）；商品/订单集群（#49）列未来项 |
| 监控 | 无主动告警，"resource 挂 2 天才发现" | #30（Prometheus，暂缓）**+ ✅ #61 已完成（2026-09-12）**：新机 cron **每 5 分钟外部端到端探活**（`e2e_probe.py`，实测 **9/9 PASS**）——不查 `/actuator/health`（那次故障里 health 全是 200），只打真实业务入口 |
| 监控 | 无日志聚合，跨服务排查逐容器翻日志 | #41（Loki 评估，暂缓） |
| 容灾 | 无自动化备份（实测只有 Docker 卷） | ✅ **#29 已完成（2026-09-08）**：每日 **02:30** 全量 dump 6 库 + 保留 7 天 + 非空校验；**✅ #47 恢复演练已完成**（独立临时容器验证） |
| 交付 | 无镜像仓库/版本 tag/CI 流水线，部署不可回滚 | #39（评估，暂缓） |
| 可靠性 | MQ 无死信队列 + 订单消费者无限 requeue | ✅ **#36 已完成（2026-09-08）**：DLX 死信 + requeue 限次；**✅ #65/#70 已完成（2026-09-12）**：库存扣减链路收口（AUTO ack + 重试 3 次 + reject 进死信 + 死信告警）；**✅ #72**：秒杀队列死信（broker policy 施加） |
| 资源 | 内存 86% 紧张、Nacos 未降堆、无 mem_limit | ✅ **R7 已完成（2026-09-07）**：Nacos 降堆 `512m` + **21 容器 `mem_limit`** + **Swap 2G**（available 2.1G → **~3.1G**） |

- **实际执行顺序（面试讲"我先做什么、为什么"——已经是过去时，不是计划）**：
  - **第一批（安全 + 资源止血，2026-09-07）**：#25 JWT 随机化（**最严重安全洞**，成本最低风险最高）→ R7 内存 → #24 MySQL/端口 → R1~R4 Redis 加固 → #38 Dockerfile 清理
  - **第二批（正确性 + 可靠性，2026-09-08）**：#33 双索引统一 → #8 预算时区 → #36 MQ 死信 → #23 校验补全 → #13 Nacos 认证 → #29 备份 + 恢复演练 → #5-P0 Sentinel 规则
  - **第三批（高可用 + 集群，2026-09-09）**：#9 Redis 主从哨兵 → #4 秒杀跨机双实例 → #14-P2 副本写保护
  - **第⑯批（2026-09-12）**：#65/#70 库存扣减链路收口 + #72 秒杀死信 + #54 MQ 凭据 + #31 向量检索开启 + #61 外部探活 + #57 Schema 核实（0 处需 ALTER）
  - **尚未做（面试讲认知）**：#30 Prometheus 告警 / #39 CI-CD / #41 日志聚合 / #26 网络隔离 / #22 CORS 收敛 / #51 restart 策略 / #45 Dubbo 名规范 / #17 登录限流·密码策略 / HTTPS（域名+备案阻塞）
- **面试话术**（已改成"已完成 + 剩余"口径）："从学习项目到生产，最大差距是 **HA + 可观测性 + 安全基线**。我没有停在'我知道该做什么'——**三批都真做了并留了实测证据**：JWT 密钥从默认值换成随机密钥（最严重的洞）、MySQL 从 4 位 'root' 换成 43 位强密码、Redis 加密码+AOF+内存上限、Nacos 开认证、数据库每日备份**并做了恢复演练**、Redis 做成**跨机主从+3 哨兵**（选主 6.1s）、秒杀做成**跨机双实例**（负载 30:30）。**剩下的我也讲得清为什么还没做**：HTTPS 卡在域名+ICP 备案；Prometheus/CI 是收益排序靠后的规模项；Nacos/ES 多副本受两台机器内存硬约束。生产化不是一次重构，是**按优先级持续逼近，并且每一步都能回滚**。"

