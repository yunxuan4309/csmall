# 10-11 面试准备 — 问题解决（提炼版 · 变更的生效性验证）

> **核心问题**: **"我做了变更"和"变更生效了"是两件事** —— 变更有**六个说谎的入口**：**多实例**（只改了一台）、**影子配置**（改的不是被订阅的那份）、**校验漏项**（只查了不会被写的字段）、**编排空跑**（命令发出去了但没执行）、**没真重建容器**（`up -d` 在解析镜像阶段就失败、容器 `Up` 时间没变）、**前置条件不成立**（同一份脚本换台机器就静默不执行）。它们**全都不报错**，而且在你的视角里都"已经完成"了：代码提交了 / 接口返回 `true` / 脚本打印 ✅ / 编排退出码 0。**唯一解药是到"生效侧"取证。**
> **覆盖 2 个点（Q29~Q30）**: `#69+#64` 双实例只重建一台 → 旧 jar 副本消费了那条消息 / Nacos 影子 dataId → PUT 返回 true 而应用行为不变 / "假通过"：校验漏了 `sales`，靠**基线比对**抓出来 / 编排脚本**静默空跑**（`r` 是 `Invoke-History` 别名）+ **看门狗** / **命令没真重建容器**（镜像 tag 回落 `latest` → 去公网拉超时） / **两机前置条件不一致**（`.env` 权限 640 / `ecs-user` 不在新机 docker 组）
> **来源**: `docs/问题解决/` 的 [[问题解决--代码与线上不一致的静默失效]] **§四 问题 3**（2026-09-12 扩充）；实测认知 **G14/G16** 见 [[Python模拟数据与数据隔离方案]] §G，执行记录见 [[TODO第三批实现与原理-2]] §七 / §八；部署纪律已固化为 `deploy/scripts/deploy-seckill-both.ps1`
> **格式**: 沿用 [[10-问题解决]] 的约定（`追问 / 简答 / 深挖 / 面试话术`）。**本系列是它的延续**（`10-1 ~ 10-11`，Q 编号连续：10-6 = Q15~Q17、10-7 = Q18~Q19、10-8 = Q20~Q22、10-9 = Q23~Q25、10-10 = Q26~Q28、**本篇 = Q29~Q30**）。
> **关联**: [[10-10-问题解决（提炼版·压测可信性）]]（**同一族：观测/工具自身会说谎**）· [[10-6-问题解决（提炼版·契约与一致性）]]（本篇是它"代码与线上不一致"那一节的**深挖**）· [[10-7-问题解决（提炼版·验证方法论）]] · [[面试速记-7 通用话术与方法论]]
> **内容说明**: 本篇是**面试用提炼版** —— 只留可直接讲的骨架；**完整过程（六个个案的现象/根因/修复/判据）看源文档**。
> ℹ️ 边界声明：[[问题解决--本地双实例分布式锁验证方法]] 讲**判据**的可信性（判据要落在失败方）、[[问题解决--压测结论的可信性（先证伪干扰项与工具自身）]] 讲**测量**的可信性；**本篇专讲"变更生效性"的验证** —— 即**部署 / 配置 / 清理脚本 / 编排脚本**这四类变更凭什么算生效了。

---

### Q29. 变更没生效的两种形态：**只改了一半** 与 **改的不是那一份**

> 来源：[[问题解决--代码与线上不一致的静默失效]] §4.1 / §4.2

【追问1】你修完 bug、重建镜像、重启服务，重跑验证**错误又出现了** —— 代码没改对？
【追问2】你在 Nacos 上改了阈值，PUT 返回 `true`、`GET` 也读到了新值，但压测里成功 RPS **恒定在旧值** —— 是限流器有 bug 吗？
【追问3】这两次有什么共性？

【简答】有共性的，而且是同一条：**我看到的是"提交侧"的成功，不是"生效侧"的事实**。**① 多实例只改了一台** —— 那个微服务是**跨机双实例**（主 + 副本），两者注册**同一个 RPC 服务**、消费**同一个 MQ 队列**（竞争消费）；我只重建了主实例，这条消息**恰好被另一台旧 jar 的副本消费**了，所以旧逻辑照旧执行（副本日志时间戳能和这次消费对上）。**② 改的不是被订阅的那份配置** —— 应用订阅的 dataId 是 `xxx-flow-rules`（**没有扩展名**），我 PUT 的是 `xxx-flow-rules.json`（**仓库文件就叫这个名**）⇒ 我在配置中心**新建了一个同名影子配置**，应用侧**从未收到通知**，`notify-ok` 的 md5 从头到尾都是旧规则的。两次都不是"改错了代码/配置"，而是**改的东西没有任何一条路径去读它**。

【深挖】
- **多实例取证要落到"每个实例"**：把"部署完成"定义成一条可执行命令，而不是一个感觉 —— 逐实例比对**容器内 jar 指纹**、宿主 jar 文件指纹、容器状态、健康检查返回码，四者全绿才算完成；**两台指纹必须相同**，不同就直接红字报警。这件事一旦脚本化（`-CheckOnly` 只读模式，随时可跑），"我明明改了"这种自我说服就没有生存空间了。
- **副本形态的坑：别把镜像 tag 写死日期** —— `docker compose build` 会**覆盖同名 tag**，于是"我明明构建了新版本"却拿到旧层；正确做法是主/副本**共用同一个镜像**，差异**收敛成环境变量**（端口 + 链路追踪的服务名），从根上消灭"两个版本并存"的土壤。
- **配置类变更的取证位在"消费侧"**：配置中心的返回码只证明**它存下了**；要证明**应用用了**，就得看应用侧的**变更通知日志（md5）**或**行为指标**。而且**改之前先确认应用订阅的 dataId/group 是哪个** —— 照应用的配置项抄，**别照仓库的文件名抄**（仓库文件名往往只是本地约定的产物）。
- **小档探针是"生效性"的最短路径**：配置改完**先跑最小的一档**（比如最小档并发 / 单次调用）—— 阈值如果真的生效了，**最小的那一档就该变**；不变就别往下压，先回来查生效性，能省掉整轮无效压测。
- **误建的影子配置当场删掉** —— 否则它迟早会被下一个人（包括未来的自己）改到。

**面试话术**
"同一天我在这上面栽了两次，而且两次都**不报错**。第一次是我修了一个微服务的缺陷、重建镜像、重启，重跑验证**错误又出现了** —— 后来查出来这个服务是**双实例**，主实例和副本**消费同一个消息队列**，我只重建了主实例，那条消息**恰好被副本的旧 jar 消费**了，所以跑的还是老逻辑。从那以后我把'部署完成'改成一条命令：**逐实例**比对容器内 jar 的 md5、宿主的 jar 文件、容器状态和健康检查，**四个都绿、而且两台指纹一致**才算完成；副本也不再单独维护镜像，而是**和主实例共用同一个镜像**、差异只留端口和追踪名两个环境变量。第二次是调并发阈值：配置中心 **PUT 返回 `true`、`GET` 也读到新值**，可压测的成功 RPS **一直卡在旧阈值** —— 根因是应用订阅的 dataId **没有扩展名**，我改的是仓库里那个带 `.json` 的同名配置，等于**在配置中心建了个影子配置**，应用侧压根没收到通知。所以我现在有一条纪律：**配置类变更不看服务端返回码，只看消费侧**（应用的变更通知日志和行为指标），而且改之前先照应用的配置项确认 dataId；改完**先跑最小的一档**当探针。"

→ 📄 **完整复盘见 [[问题解决--代码与线上不一致的静默失效]] §四 问题 3**

---

### Q30. 观测与自动化**自己也会说谎**："假通过"与"编排空跑"，以及变更生效性的六个检查位

> 来源：[[问题解决--代码与线上不一致的静默失效]] §4.3 / §4.4 / §4.5 / §4.6 / §4.7（🆕 2026-09-12 扩充：个案 E「命令跑了但在重建容器之前就失败」/ 个案 F「同一份脚本在一台机器上静默什么都没做」+ 小结「**六个检查位**」）

【追问1】你的清理脚本每次都打印「✅ 全部回位 / 校验通过」，你凭什么还去查数据？
【追问2】一段 5 步的远程编排"执行完成"了，怎么会出现"一步没执行"？
【追问3】那判断一次变更到底有没有生效，你现在怎么做？

【简答】**① "假通过"**：脚本的校验**只覆盖了库存表**，而实际被写坏的是**销量（累加字段）** —— **校验集不是变更集的超集**，那个 ✅ 是**假的**，而且比没有校验更危险（它让我**停止了怀疑**）；最后是靠**造数前存基线、清理后比对基线**（11 张表的行数 + 两个累加量合计）才抓出来，顺带还修掉了"全局口径跨批次必然误报"的问题。**② "编排空跑"**：那段编排在 PowerShell 里用了单字母函数名 `R`，而 **`r`/`R` 是 `Invoke-History` 的内置别名**，我的函数**被别名优先解析**，`R ...` 变成了"调用历史命令" ⇒ **整段静默空跑**，连"还原"那一步也没执行，生产就这么停在了临时状态上（事后手工还原并逐项复核）。**③ 现在我用六个检查位**：**落到几个实例 / 改的是不是被订阅的那份 / 校验是否覆盖了所有会被写的字段 / 编排有没有真的跑完 / 命令有没有真的重建容器 / 前置条件在每台机器上是否都成立**（后两条是 2026-09-12 扩充的个案 E/F 换来的：⑤ 看 `up -d` 输出里有没有 `✘`/`pull`/`timeout`、容器 `Up` 时间有没有变 —— 镜像 `tag` 变量没落到 `.env` 就会回落 `latest`，新机本地没有 `latest` ⇒ 去公网拉超时，容器**其实没重建**而 `docker ps` 一切正常；⑥ 看远程脚本要读的 `.env`、要用的组/权限、要用的镜像在**每台**机器上是否都满足 —— 新机 `.env` 是 `640 ecs-user:ecs-user`、`ecs-user` 又不在新机 docker 组，同一份脚本在新机就**静默什么都没做**）。

【深挖】
- **"校验通过"必须能回答"校验覆盖了哪些字段"** —— 答不上来的 ✅ 一律当**假通过**处理。做法上有个兜底：对**会被写到的聚合量**做**前后基线比对**（行数 + `SUM`），而不是只做"写得对不对"的点检；因为"漏检"是无法靠"更仔细地看同一份校验"发现的。
- **校验口径也有时点与范围问题**：全局口径在校验"某一次批次的清理"时**必然误报**（别的批次的数据也算进来）⇒ 校验要**按批次/按作用域**收敛，这与"分母要按批次"是同一个教训。
- **脚本要能自证执行过**：每一步打印 + 关键返回值 + 幂等；**别用单字母或易撞别名的函数名**（先查别名表），否则你连"它跑没跑"都不知道（PowerShell 的别名是**内置优先级高于自定义函数**的）。
- **有副作用的编排必须配看门狗（事前，不是事后）**：**先武装"定时自动还原"**，再开始有副作用的步骤 —— 这样即使编排中断、被杀、或像上面那样空跑，**最终状态也会自己回到基线**。这一次是事后补的手工还原，**下一次必须是事前就位**。
- **长脚本的"完成"以独立复核为准**：对**生产最终状态**做一次独立取证（AI 指向 / 开关 / 阈值 / 进程 / 容器数），而不是相信脚本自己的打印 —— 这和"压测机要放在被测系统之外"是同一个思路：**别让被验证的对象自己出具证明**。
- ⭐ **⑤ "命令跑了" ≠ "容器被重建了"（个案 E）**：`docker compose up -d` 的失败可能发生在**最前面两步** —— 连不上 docker API（`permission denied`），或者**解析镜像**时就退出（`✘ Image … Error … registry-1.docker.io … i/o timeout`）。而 `up -d` 失败**不会动已有容器** ⇒ `docker ps` 里容器照样 `Up`、HTTP 照样 200，**从可用性角度看"什么都没发生"**。本例的双重坑：① `image: csmall-mall-seckill:${MALL_SECKILL_TAG:-latest}` 的变量**两台机器的 `.env` 里都没有** ⇒ 任何不带变量的 `up -d` 都在用 `latest`；老机恰好有历史 `latest` ⇒ "重建成功"，新机只有 `:20260912` ⇒ 回落 `latest` ⇒ 去公网拉 ⇒ 超时 ⇒ **容器根本没重建**（副本带着旧凭据跑了 3 小时，MQ 消费悄悄退化成单实例、外部零症状）；② `permission denied` 把注意力带偏（去修权限），而真正让变更失效的是**后一层镜像 tag**。**判据**：`up -d` 输出要看全（`✘`/`Image ... Error`/`pull`/`timeout` 一出现就是不妙），**容器的 `Up` 时间不会变**就别当"变更完成"；`grep -E '_TAG=' .env` 确认镜像名里的 `${VAR:-default}` 有落点。
- ⭐ **⑥ 前置条件必须在"每台机器"上都成立（个案 F）**：同一份部署脚本，老机跑完全绿、**新机静默什么都没做** —— 唯一线索是两行 `open /data/csmall/.env: permission denied`，**没有构建日志、容器没被重建**。根因是**两机权限口径漂移**：远程命令以 `ai-deepseek` 身份执行（无 sudo），而新机 `/data/csmall/.env` 是 `640 ecs-user:ecs-user`、老机是 `664` ⇒ 新机读不到 ⇒ `docker compose` 连变量文件都打不开、在解析配置阶段就退出（**"容器还在跑"再次制造假象**，与个案 E 同一机理）。**处置**：新机 `chgrp docker` + `chmod 640`（口径比老机更窄），并在脚本里加**前置自检** `test -r /data/csmall/.env`。**通用判据**：远程编排脚本先自检文件可读性 / 组归属 / 镜像是否存在 / 目录可写；同一份配置在两台机器上要有**一致的权限口径**（`.env`、`jars/`、`dockerfiles/`、组成员）。
- ⭐ **一句话**：**别问"我改了吗"，要问"生效侧现在是什么"** —— 版本看**逐实例指纹**、配置看**消费侧日志**、数据看**前后基线**、流程看**独立复核**、**依赖看连接日志**（`delegate=amqp://<user>@<host>` + `ACCESS_REFUSED` 计数为 0）。

**面试话术**
"我给自己抓到过两个更'元'的问题：**验证手段自己会说谎**。第一个是我写的数据清理脚本，跑完打印「✅ 校验通过」，但我每次核对都发现**销量总和比基线多 1** —— 因为它的校验**只覆盖库存表、没覆盖销量**这个会被累加的字段，也就是说**校验集不是变更集的超集**，那个 ✅ 是假的。这类"假通过"比"没有校验"更危险，因为它会让你**停止怀疑**；我最后的解药不是"更仔细地看同一份校验"，而是**造数前存一份基线**（11 张表的行数加两个累加量合计），清理后**比对基线**，任何一项不符就报错 —— 顺便还修掉了"全局口径跨批次必然误报"的毛病。第二个更离谱：我写了一段 5 步的远程编排（备份 → 切流量 → 压测 → 还原 → 复核），执行完看着正常，复核时发现**生产还停在临时状态** —— **还原那一步没跑**，整段编排**一步都没执行**。根因是 PowerShell 里 `r` 和 `R` 是 `Invoke-History` 的内置别名，把我自定义的函数名覆盖了，命令变成了"调用历史命令"，**静默空跑**。所以我现在对'变更是否生效'有六个固定检查位：**① 落到几个实例（逐实例核 jar 指纹）② 改的是不是被订阅的那份（看消费侧日志，不看服务端返回码）③ 校验有没有覆盖所有会被写的字段（含累加量，用基线比对兜底）④ 编排有没有真的跑完（独立复核，而且有副作用的编排要事前武装看门狗还原）⑤ 命令有没有真的重建容器（看 `up -d` 输出里有没有 `✘`/`pull`/`timeout`、容器 `Up` 时间有没有变 —— 我们的镜像 `tag` 变量没落到 `.env`，回落 `latest` 后新机去公网拉超时，容器**根本没重建**，而 `docker ps` 里一切正常）⑥ 前置条件在每台机器上都成立（远程脚本要读的 `.env`、要用的组/权限、要用的镜像，每台都得满足 —— 新机 `.env` 是 `640 ecs-user:ecs-user`、`ecs-user` 又不在新机 docker 组，同一份脚本在新机就**静默什么都没做**）**。"

→ 📄 **完整复盘见 [[问题解决--代码与线上不一致的静默失效]] §四 问题 3**

---

**维护提示**: 本篇是**变更生效性**这一类的面试提炼稿；六个个案的完整证据（含逐实例 md5 命令、影子 dataId 的 md5 对照、基线比对口径、别名陷阱、镜像 tag 回落 `latest` 的 `up -d` 原文、两机 `.env` 权限对照）见 [[问题解决--代码与线上不一致的静默失效]] §四（§4.1~§4.7）；实测认知 **G14/G16** 与两条部署纪律见 [[Python模拟数据与数据隔离方案]] §G 与 `deploy/scripts/deploy-seckill-both.ps1` 文件头；基线比对工具为 `deploy/scripts/sim/simulate_data.py --baseline / --compare-baseline`。
