# CoolShark 开发环境中间件配置清单

> 最后更新：2026-07-30
> 覆盖本地开发所需全部中间件的版本、端口和账号信息。生产部署配置见 `docs/阿里云ECS服务器情况.md` 和 `docs/问题解决--部署阶段.md`。

---

## MySQL

| 配置项 | 值 |
|--------|-----|
| 版本 | 8.0（Windows 本地） |
| 端口 | 3306 |
| 账号/密码 | root / root |
| 数据库列表 | cs_mall_pms, cs_mall_oms, cs_mall_ams, cs_mall_ums, cs_mall_seckill, cs_mall_resource |
| 连接池 | HikariCP（Spring Boot 默认，**不使用 Druid**） |
| 版本管理 | **Flyway**（启动自动迁移，SQL 文件在 `db/migration/` 下） |

---

## Redis

| 配置项 | 值 |
|--------|-----|
| 版本 | 3.2.100（Windows 本地） |
| 端口 | 6379 |
| 密码 | 无 |
| 说明 | Windows 版不支持 RedisBloom 模块，秒杀用 Redis Set 替代布隆过滤器 |

---

## Nacos

| 配置项 | 值 |
|--------|-----|
| 版本 | 2.5.2 |
| 端口 | 8848 |
| 部署模式 | 单机模式 |
| 认证 | 无（本地开发） |
| 用途 | 服务注册 + 发现 + 配置中心 |

---

## Seata

| 配置项 | 值 |
|--------|-----|
| 版本 | 2.1.0（Apache Seata） |
| RPC 端口 | 8091 |
| 控制台端口 | 7091 |
| 账号/密码 | seata / seata |
| 注册中心 | Nacos |
| 模式 | AT 模式（`@GlobalTransactional`） |

---

## Sentinel

| 配置项 | 值 |
|--------|-----|
| 版本 | 1.8.6（Dashboard JAR） |
| Dashboard 端口 | **8090**（从 8080 改为 8090，避免和 SkyWalking/Spring Boot 默认端口冲突） |
| 账号/密码 | sentinel / sentinel |
| Dashboard JAR | `deploy/sentinel-dashboard/sentinel-dashboard.jar` |
| 启动方式 | IDEA Run Configuration (Sentinel Dashboard) 或 `java -jar -Dserver.port=8090` |
| 接入模块 | mall-gateway, mall-order, mall-product, mall-front, mall-search, mall-ai, mall-seckill |
| 规则持久化 | 当前内存存储，生产需配置 Nacos 持久化 |

---

## RabbitMQ

| 配置项 | 值 |
|--------|-----|
| 版本 | 4.3.0（Windows 本地） |
| AMQP 端口 | 5672 |
| 管理界面 | `http://localhost:15672` |
| 账号/密码 | guest / guest |
| 用途 | 秒杀异步落库 + 普通订单 MQ 异步扣库存 |
| 注意事项 | Windows 计算机名必须纯英文，需设置 `RABBITMQ_NODENAME=rabbit@localhost` |

---

## Elasticsearch

| 配置项 | 值 |
|--------|-----|
| 版本 | 8.6.0（Windows 本地） |
| HTTP 端口 | 9200 |
| 传输端口 | 9092 |
| 安全设置 | 已关闭密码验证 |
| 分词器 | IK 中文分词 |
| 用途 | 商品全文搜索 + AI 导购 RAG 检索 |

---

## SkyWalking

| 配置项 | 值 |
|--------|-----|
| 版本 | 9.7.0（APM）/ Agent 9.4.0 |
| OAP gRPC 端口 | 11800 |
| OAP HTTP 端口 | 12800 |
| UI 端口 | **8088**（从默认 8080 改为 8088，避免和 Sentinel Dashboard 冲突） |
| 启动方式 | IDEA Shell Script `start-skywalking.bat` 或双击 `deploy/skywalking/apache-skywalking-apm-bin/bin/startup.bat` |
| 接入模块 | mall-gateway, mall-order, mall-product |
| 启动顺序 | **必须先启动 OAP + UI，再启动微服务**，否则 Agent 超时阻塞 |

---

---

## 端口汇总

| 端口 | 服务 |
|------|------|
| 3306 | MySQL |
| 5672 | RabbitMQ AMQP |
| 6379 | Redis |
| 7091 | Seata 控制台 |
| 8088 | **SkyWalking UI** |
| 8090 | **Sentinel Dashboard** |
| 8091 | Seata RPC |
| 8848 | Nacos |
| 9200 | Elasticsearch HTTP |
| 11800 | SkyWalking OAP gRPC |
| 12800 | SkyWalking OAP HTTP |
| 15672 | RabbitMQ 管理界面 |

---

## 微服务端口汇总

| 端口 | 服务 | 说明 |
|------|------|------|
| 9060 | mall-resource | 资源服务（文件上传） |
| 10003 | mall-ams | 后台管理服务 |
| 10004 | mall-front | 前台 Dubbo 消费服务 |
| 10005 | mall-order | 订单服务（RabbitMQ + Seata） |
| 10006 | mall-ums | 用户管理服务 |
| 10007 | mall-seckill | 秒杀服务（RabbitMQ + Seata） |
| 10008 | mall-search | 搜索服务（ES + Seata） |
| 10009 | mall-sso | 单点登录（双数据源） |
| 10010 | mall-ai | AI 导购（ES + RAG） |
| 10087 | mall-gateway | API 网关入口 |

---

## 启动顺序

```
1. 中间件：MySQL → Redis → Nacos → RabbitMQ → ES → Seata
2. 可观测性：SkyWalking (startup.bat) → Sentinel Dashboard (IDEA 8090)
3. 独立服务：mall-resource → mall-ams → mall-ums
4. Dubbo 提供者：mall-product
5. Dubbo 消费者：mall-front → mall-order → mall-search → mall-ai
6. 秒杀：mall-seckill
7. 网关入口：mall-sso → mall-gateway-server
```
