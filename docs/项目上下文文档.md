# CoolShark 微服务电商平台 — 项目上下文文档

> **更新日期**: 2026-09-09（**新增第二台服务器 csmall-node2 跨机集群实验环境**：2C8G 同 VPC，用于 Redis 从+3哨兵/秒杀副本/造数执行位；附录 A 服务器信息已扩充为两台；#46 nacos 数据卷 + #47 备份恢复演练已完成；**第三批主线 = 跨机集群 #4/#9/#14-P2，阶段 0~6 执行清单见 [[TODO文件]] 顶部，§九 已同步最新状态**）
> **重构说明**: 本文档基于项目内多份分散的 Markdown 文档重新整合编写。合并了 PROJECT_CONTEXT.md、TODO文件.md、CLAUDE.md 以及 docs/ 目录下 20+ 份文档的内容，删除了已过时的生产环境细节和已实现功能的详细接口文档，保留了所有踩坑记录和问题解决方案。项目已部署至阿里云 ECS（Docker 容器化，21 个容器稳定运行），已接入 SkyWalking 全链路追踪。**docs/ 目录结构**：`docs/评估报告/`（TODO、TODO已完成、各批次实现与原理、评估/审计报告）、`docs/运维/`（服务器运维手册 + 敏感文件，gitignore）、`docs/归档/`（已执行/过时文档）、`docs/问题解决/`、`docs/面试准备/`、根目录（主文档/方案/指南）。
> **上一批更新（2026-09-08）**: 第二批全量部署服务器——#33 双索引统一（AI 索引 19 条、mall-search 只读降级层、前端 fallback）、#8 AI 预算北京时间、#36 订单 DLX+requeue 限次、#23 校验补全、#14 秒杀防数据 P0+P1（order_type Flyway V6 + 方案Y + 对账任务）；部署中修复 mall-ai reasoning 模型 JSON 截断 bug（JSON 任务换 deepseek-chat）。**第二批已全部完成**，其余入第三批。

---

## 一、项目概述

### 1.1 项目背景

CoolShark（酷鲨）是一个基于 Spring Cloud Alibaba 的微服务电商平台，面向 C 端用户提供商品浏览、搜索、购物车、下单、支付、秒杀等完整购物流程，同时提供后台管理系统进行商品、订单、用户管理。此外集成了 AI 智能导购模块，支持自然语言商品问答、多商品对比和多轮对话导购。

**当前状态**: Docker 容器化部署完成，21 个容器（新增 SkyWalking OAP + UI）稳定运行于阿里云 ECS（**老机** 4C16G，Ubuntu 24.04，公网 IP: 8.156.77.197）。

**2026-09-09 新增：第二台服务器（新机 `csmall-node2`，2C8G，公网 47.109.70.197 / 私网 172.29.193.240）** —— 与老机**同 VPC 同安全组**（内网互通实测 0.43ms），用于**跨机集群实验**：Redis 从 + 3 哨兵、秒杀副本(10017)、造数/压测执行位。属 TODO 第三批 #4/#9/#14-P2/#42 范畴，方案见 [[跨机集群方案-讨论纪要-2026-09-09]]。⚠️ 前置：老机 MySQL(3306)/Redis(6379) 绑 `127.0.0.1`（第一批安全加固）→ 跨机不可达，需端口放行改造。

> ✅ **2026-09-09 晚：跨机集群已建成（阶段 0~5 完成）**——老机 = **Redis 主(6379)** + **秒杀实例1(10007)**；新机 = **Redis 从(6380) + 3 哨兵(26379-26381, quorum=2)** + **秒杀实例2(10017)**；端口放行已完成（3306/6379/20880/20881 绑私网 IP，无需改安全组）。实测：**故障转移选主 6.1s / 客户端 9.1s 自愈 / 数据零丢失**；**负载分布 60 请求精确 30:30**；**定时任务锁互斥验证通过**；停实例剔除通过（⚠️ 16s 失败窗口见 TODO #53）。**#14-P2 亦已完成**（`min-replicas-to-write 1`，运行时 09-09 + conf 落盘 09-10）→ **跨机集群主线 100% 收口**。详见 [[TODO已完成]] §十三、[[TODO第三批实现与原理]] §五/§六。

### 1.2 项目仓库

| 项目 | 仓库地址 | 本地路径 |
|------|---------|---------|
| 后端 | https://github.com/yunxuan4309/csmall.git | D:\java\csmall |
| 前端 | https://github.com/yunxuan4309/csmall-vue.git | D:\Vue-Workspace\csmall-vue |

---

## 二、技术栈

### 2.1 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Java (OpenJDK) | 21 | 开发语言 |
| Spring Boot | 3.2.5 | 基础框架 |
| Spring Cloud | 2023.0.3 | 微服务框架 |
| Spring Cloud Alibaba | 2023.0.1.2 | Nacos/Sentinel/Seata 集成 |
| Apache Dubbo | 3.3.2 | RPC 框架（替代 Feign） |
| MyBatis-Plus | 3.5.9 | ORM 框架 |
| Seata | 2.1.0 (Apache) | 分布式事务 |
| Sentinel | (由 BOM 管理) | 流量控制 |
| Nacos | 2.5.2 | 注册中心 + 配置中心 |
| MySQL | 8.0 | 关系型数据库 |
| Redis | 3.2.100 (Windows 开发) | 缓存 |
| RabbitMQ | 4.3.0 (Windows 开发) | 消息队列 |
| Elasticsearch | 8.6.0 (Windows 开发) | 全文搜索 |
| HikariCP | (Spring Boot 自带) | 数据库连接池 |
| Knife4j | 4.5.0 | API 文档 |
| DeepSeek V4 Flash | API 调用 | AI 对话模型 |
| SkyWalking | 9.7.0 (OAP) / Agent 9.4.0 | 全链路追踪 |

### 2.2 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.5 | 前端框架 (Composition API) |
| Vite | 7 | 构建工具 |
| Element Plus | 2.13.6 | UI 组件库（PC + 响应式适配移动端） |
| Pinia | 3 | 状态管理 |
| Vue Router | 4 | 路由管理 |
| Axios | 最新 | HTTP 客户端 |

---

## 三、模块架构

### 3.1 服务模块总览

| 模块 | 端口 | 数据库 | 职责 |
|------|------|--------|------|
| mall-gateway-server | 10087 | 无 | API 网关，路由转发，认证 |
| mall-sso | 10009 | cs_mall_ams + cs_mall_ums | 统一登录认证 |
| mall-product | 9010 | cs_mall_pms | 商品管理 (SPU/SKU/品牌/分类) |
| mall-front | 10004 | 无 (Dubbo 消费) | 前台商品展示 |
| mall-search | 10008 | ES | 商品搜索 |
| mall-order | 10005 | cs_mall_oms | 购物车/订单/支付 |
| mall-seckill | 10007 | cs_mall_seckill | 秒杀活动 |
| mall-ums | 10006 | cs_mall_ums | 用户管理/地址/积分 |
| mall-ams | 10003 | cs_mall_ams | 后台管理员/角色/权限 |
| mall-resource | 9060 | cs_mall_resource | 文件上传/图片管理/用户上传记录 |
| mall-ai | 10010 | 无 (ES + Dubbo 消费) | AI 智能导购：商品对比、RAG 问答、多轮对话 |

### 3.2 基础库模块（不可独立启动）

| 模块 | 职责 |
|------|------|
| mall-common | 公共组件：统一异常、响应封装、JWT 工具、Redis 配置、Seata Dubbo Filter 等 |
| mall-pojo | 数据模型：实体类、DTO、VO |
| mall-*-service | 各业务模块的接口和实现（Dubbo 提供者），如 mall-product-service 等 |

### 3.3 已弃用模块

| 模块 | 状态 | 说明 |
|------|------|------|
| mall-leaf | 已弃用 | 美团 Leaf 分布式 ID 生成器，改用 MyBatis-Plus `IdWorker.getId()`（雪花算法） |

**弃用原因**: Leaf 基于 JDK 8 + Spring Boot 2.5.4，与项目其他模块（JDK 21 + Spring Boot 3.2.5）存在代差，无法统一构建。美团 Leaf 自 2020 年起已停止维护。

### 3.4 Dubbo 服务依赖关系

```
                    ┌──────────────────────┐
                    │  mall-product-webapi  │  ← 核心 Dubbo 提供者 (端口 9010)
                    │  7 个 Dubbo 接口      │
                    └───┬──────┬──────┬────┘
                        │      │      │
             ┌──────────┘      │      └──────────┐
             ▼                  ▼                  ▼
  ┌──────────────────┐ ┌──────────────┐ ┌──────────────────┐
  │ mall-front-webapi │ │ mall-order-  │ │ mall-search-     │
  │   (端口 10004)    │ │ webapi       │ │ webapi           │
  └──────────────────┘ │ (端口 10005) │ │ (端口 10008)     │
                       └──────┬───────┘ └────────┬─────────┘
                              │                  │
                              ▼                  ▼
                       ┌──────────────────┐ ┌──────────────────┐
                       │ mall-seckill-    │ │   mall-ai-       │
                       │ webapi           │ │   webapi         │
                       │ (端口 10007)     │ │ (端口 10010)     │
                       └──────────────────┘ └──────────────────┘

独立服务（无 Dubbo 依赖）：mall-resource(9060), mall-ams(10003), mall-ums(10006)
入口服务：mall-sso(10009), mall-gateway-server(10087)
```

mall-product-webapi 提供的 Dubbo 接口：
- `IForFrontSpuService` — 前台商品 SPU 查询
- `IForFrontSkuService` — 前台商品 SKU 查询
- `IForFrontCategoryService` — 前台分类查询
- `IForFrontAttributeService` — 前台属性查询
- `IForOrderSkuService` — 订单扣减库存
- `IForSeckillSpuService` — 秒杀 SPU 查询
- `IForSeckillSkuService` — 秒杀 SKU 查询
- `ISpuSyncService` — 商品变更同步到 ES（mall-product → mall-ai 触发）

### 3.5 启动顺序

1. **基础设施**: Nacos → MySQL → Redis → Seata → RabbitMQ → Elasticsearch → Sentinel
2. **独立服务**（可并行）: mall-resource, mall-ams, mall-ums
3. **Dubbo 提供者**: mall-product（核心，必须在消费者之前启动）
4. **Dubbo 消费者**（可并行）: mall-front, mall-order, mall-search, mall-ai
5. **依赖消费者**（等 order/product 就绪）: mall-seckill
6. **网关注册**: mall-sso → mall-gateway-server（最后启动）

---

## 四、数据库清单

| 数据库 | 用途 | 表结构位置 | 表数 |
|--------|------|-----------|------|
| cs_mall_pms | 商品库 (SPU/SKU/品牌/分类/相册/属性) | `database/cs_mall_pms/` | 14（13 业务 + undo_log）|
| cs_mall_ams | 管理员库 (管理员/角色/权限/登录日志) | `database/cs_mall_ams/` | 6 |
| cs_mall_oms | 订单库 (订单/订单项/购物车) | `database/cs_mall_oms/` | 5（4 业务 + undo_log）|
| cs_mall_ums | 用户库 (用户/地址/积分/登录日志) | `database/cs_mall_ums/` | 2 |
| cs_mall_seckill | 秒杀库 (秒杀SPU/SKU/成功记录) | `database/cs_mall_seckill/` | 5（4 业务 + undo_log）|
| cs_mall_resource | 文件上传记录 | `database/cs_mall_resource/` | 1 |

> **2026-08-28 表数核对（Flyway 迁移为准）**：业务表共 **30** 张（pms 13 + ams 6 + oms 4 + seckill 4 + ums 2 + resource 1），另 undo_log 3 张（pms/oms/seckill）+ 各库 flyway_schema_history 6 张 → 全表 **39**。`database/` 导出目录可能滞后（当前缺 `seckill_message_retry`），以各模块 `db/migration/V*.sql` 为准。
> 每个数据库一个子文件夹，内含该库所有表的 `CREATE TABLE` 语句（从 MySQL 实时导出）。测试数据文件：`init-test-data.sql`、`07-seckill-test-data.sql`。

**初始化方式**: 
- 命令行: `mysql -u root -p` 后依次 `source` 各 SQL 文件
- 批处理: 运行项目根目录 `init-database.bat`
- 编码: 所有数据库统一 utf8mb4，存储引擎 InnoDB

**图片存储策略**: 数据库中只存纯相对路径（如 `spu_1_1.jpg`），由服务层 `ImageUrlPrefixHelper` 根据当前环境的 `resource-host` 配置自动拼接完整 URL。

**默认管理员账号**:
- 用户名: `admin`，密码: `123456`（BCrypt 哈希存储）
- 用户名: `liucs`（运营管理员），密码: `123456`
- 用户名: `wangkj`（客服管理员），密码: `123456`

---

## 五、本地开发环境配置

### 5.1 开发环境信息

| 配置项 | 值 |
|--------|-----|
| 操作系统 | Windows 11 Home |
| IDE | IntelliJ IDEA |
| Redis 客户端 | Another Redis Desktop Manager |
| MQ 管理界面 | http://localhost:15672 (guest/guest) |
| Docker Desktop | 已安装但 WSL2 未就绪，暂不可用 |

### 5.2 必需中间件

| 中间件 | 本地版本 | 端口 | 说明 |
|--------|---------|------|------|
| MySQL | 8.0 | 3306 | root/root |
| Redis | 3.2.100 (Windows) | 6379 | 无密码，不支持 RedisBloom 模块 |
| Nacos | 2.5.2 | 8848 | 单机模式，无认证 |
| Seata | 2.1.0 | 8091 (RPC) / 7091 (控制台) | seata/seata，注册中心指向 Nacos |
| Sentinel | 1.8.9 | 8080 | sentinel/sentinel |
| RabbitMQ | 4.3.0 | 5672 / 15672 | guest/guest，需 RABBITMQ_NODENAME=rabbit@localhost |
| Elasticsearch | 8.6.0 | 9200 / 9300 | 已关闭密码验证，已安装 IK 分词器 |

### 5.3 配置要点

1. **环境变量**: 前端 `.env.development` 中定义 6 个微服务地址（`VITE_API_SSO/GATEWAY/UMS/FRONT/SECKILL`），均指向 `http://localhost:{端口}`
2. **Spring Profile**: 默认激活 `test`，各模块 `application.yml` 中 `spring.profiles.active: test`
3. **敏感信息**: 本地开发可直接写在 yml 中（root/root），生产环境通过环境变量注入
4. **Nacos 注册中心**: 本地开发使用手动路由（`locator.enabled: false`），与生产环境对齐
5. **Knife4j**: 本地开发可访问，`knife4j.production: false`

### 5.4 常见启动问题速查

| 问题 | 原因 | 解决 |
|------|------|------|
| RabbitMQ 无法启动 | Windows 计算机名含中文 | 修改计算机名为纯英文，设置 `RABBITMQ_NODENAME=rabbit@localhost` |
| Redis 不支持 RedisBloom | Windows Redis 3.2.100 无此模块 | 秒杀用 Redis Set 替代布隆过滤器（生产环境需切换回） |
| Docker Desktop 不可用 | WSL2 未就绪 | 本地直接用 Windows 原生中间件 |
| Maven 编译报"不支持发行版本 21" | IDEA 内置 Maven 使用 JDK 17 | IDEA → Settings → Build → Maven → JDK 改为 21 |

---

## 六、关键设计决策

### 6.1 架构决策

1. **RPC 选型**: 使用 Apache Dubbo 3.3.2 替代 OpenFeign，性能更优，支持多种序列化协议
2. **分布式事务**: Seata AT 模式，`@GlobalTransactional` 注解标注核心业务流程
3. **ID 生成**: MyBatis-Plus `IdWorker`（雪花算法），自定义 XML INSERT 需手动调用 `IdWorker.getId()`（已弃用 mall-leaf）
4. **认证方式**: SSO 统一登录 + JWT Token + Spring Security 6.x，无状态设计
5. **API 文档**: Knife4j (OpenAPI 3)，生产环境 `knife4j.production: true` 屏蔽
6. **Long 精度**: 全局 Jackson 配置 `Long → String` 序列化，避免 JS 精度丢失（雪花 ID 为 19 位）
7. **Seata Dubbo Filter**: 自定义实现替代官方 bug 版本（`dubbo-filter-seata:1.0.2` 存在 branchType NPE）
8. **连接池**: 混合使用——**mall-resource 用 HikariCP**（曾因 Druid `max-wait=0` 无限等待导致上传线程挂死，2026-06 切换）；**其余 6 个服务（product/order/seckill/ums/ams/sso）仍为 Druid 1.2.24**（prod yml 显式 `type: DruidDataSource`，稳定运行）。统一到 HikariCP 为长期规划（需全量回归，暂缓）。详见 `docs/评估报告/TODO文件.md` R5

### 6.2 秒杀架构

- **库存预热**: Redis 预扣库存，减少数据库压力
- **异步落库**: RabbitMQ 消息队列异步创建订单，实现削峰
- **防缓存穿透**: 当前本地开发用 Redis Set 替代布隆过滤器（Windows Redis 不支持 RedisBloom），生产环境需切换
- **防超卖**: SQL 扣减库存加 `AND seckill_stock >= #{quantity}` 条件
- **消息积压保护**: RabbitMQ JSON 序列化替代 Java 序列化，`basicNack` 失败后抛出 RuntimeException 触发事务回滚

### 6.3 图片 URL 策略

- **存储**: 数据库只存纯相对路径（如 `spu_1_1.jpg`），不存完整 URL
- **拼接**: 服务层 `ImageUrlPrefixHelper`（`mall-product-webapi/.../utils/`）根据环境 `resource-host` 配置动态拼完整 URL
- **静态服务**: 生产环境 Nginx 直接从 `/data/csmall-upload/` 提供图片（`~* \.(jpg|png|gif|webp)$`），不经过 Java 服务栈
- **已上传图片兼容**: 工具类自动跳过已含 `http://` 前缀的路径

### 6.4 AI 智能导购架构

**搜索流水线**: 意图提取 → AI 查询扩展 → 多路召回 → 融合排序 → 流式生成

| 阶段 | 实现 | 说明 |
|------|------|------|
| 意图提取 | DeepSeek V4 Flash 解析用户意图 → 结构化 JSON | 替代正则猜预算，根治"降低2000左右"误判 |
| 查询扩展 | DeepSeek 实时扩展同义词 | "衣服" → "男装 女装 服装 鞋类" |
| 多路召回 | 关键词(IK `multi_match`) + 全文兜底 | 10 条/路，合并去重 |
| 向量检索 | 硅基流动 BGE-M3 (1024维) + ES `cosineSimilarity` | 免费，配置开关 `embedding-enabled` |
| 流式输出 | SSE (`/ai/chat/stream`) + 前端逐字渲染 | 用户实时看到 AI "打字" |
| 思考可视化 | 前端 thinking 卡片动画 | 展示流水线每个阶段 |

**核心依赖**:
- **对话模型**: DeepSeek V4 Flash（chat-completion API）
- **Embedding**: 硅基流动 BAAI/bge-m3（免费，OpenAI 兼容）
- **会话管理**: Redis 存储多轮对话上下文（TTL 过期）
- **成本控制**: `TokenBudgetService` — Redis 日计数器，全局 2 元/天预算上限
- **商品同步**: mall-product 增改商品后通过 Dubbo `ISpuSyncService` 自动通知 mall-ai 更新 ES 索引

### 6.5 环境配置管理

- Spring Boot Profile 机制：`application.yml`（基础配置）+ `application-test.yml`（本地开发）+ `application-prod.yml`（生产）
- 生产敏感信息通过环境变量注入：`MYSQL_USERNAME/PASSWORD`、`REDIS_PASSWORD`、`RABBITMQ_USERNAME/PASSWORD`、`AI_API_KEY` 等
- 各模块独立配置文件，互不影响

### 6.6 Redis 一致性策略：不使用 Redis 事务（2026-08-21 确认）

**结论**：全项目**未使用 Redis 事务（MULTI/EXEC）**。排查证据（全仓库代码检索）：无 `multi()/exec()/watch()/unwatch()/discard()`、无 `SessionCallback`、无 `executePipelined`；各模块 `RedisTemplate` 配置类（`RedisCustomConfiguration` / `RedisConfiguration`）仅配置序列化器，**未开启 `setEnableTransactionSupport(true)`**，因此 `@Transactional` 也不会驱动 Redis 事务。

**当前原子性方案**（全部为单命令原子操作）：

| 场景 | 方案 | 说明 |
|------|------|------|
| 秒杀下单锁 | `setIfAbsent`（SETNX） | 防并发重复下单，1 分钟过期 |
| 秒杀库存扣减/回补 | `increment` / `decrement` | 单命令原子，`leftStock < 0` 判断防超卖 |
| 幂等控制 | `setIfAbsent` | `IdempotentAspect` |
| token 黑名单 | Set `add` / `isMember` | 各模块 `SSOFilter` |
| AI 预算日计数 | `increment` | `TokenBudgetService` |
| 秒杀随机码/购买标记/未支付标记 | `set` / `delete` | 单 key 写入 |
| **定时任务分布式锁（集群化）** | `setIfAbsent` 加锁 + **Lua CAS 释放** | `RedisLockUtils`（mall-seckill）：双实例下防 `MessageRetryTask` / `SeckillReconcileTask` 并发双跑 |

秒杀多 key 联动（orderLock / ordered / stock / reseckill）采用**"分布式锁 + 失败补偿"**保证最终一致（下单失败/库存不足时 `increment()` 回补库存 + `delete` 释放锁），而非 Redis 事务。

**定时任务分布式锁（2026-09-09 新增，#4 秒杀集群化前置）**：秒杀双实例后 `@Scheduled` 会在**两个实例同时触发**——`MessageRetryTask`（扫表重发 MQ）两实例同时 `selectPending` 会拿到同一批记录 → **重复投递**；`SeckillReconcileTask` 会重复对账（"连续同向差计数"被加倍累计）。本地锁（`synchronized`）跨不了 JVM → 用 `mall-seckill/utils/RedisLockUtils`：`SET key token NX EX ttl` 原子加锁 + TTL 兜底 + **Lua 比较 token 再删**（防误删他人锁）；锁只保证"不并发"，**正确性仍靠幂等**（`updateStatusSent`）+ DB 条件扣减 + 唯一索引兜底。
> **选型**：未用 Redisson（`mall-seckill-webapi/pom.xml` 已引 3.24.3 但**代码零使用**，属死依赖）——场景只需非阻塞互斥，而 Redisson 带来可重入/看门狗/Redlock/连接池（默认 64）与另一套哨兵配置 API；也未用 ShedLock（更贴合 `@Scheduled` 但需引依赖 + 配 provider）。演进条件见 [[TODO第三批实现与原理]] §4.7。

**⚠️ Redis 高可用局限（2026-09-09，务必知悉）**：生产 Redis 是**跨机 1 主 1 从 + 3 哨兵**（老机主 / 新机从），并已开启 `min-replicas-to-write 1`（**只开主库**）。**由此带来一个已知局限**：**新机（从库）不可用时，老机主库会拒绝所有写**（`NOREPLICAS Not enough good replicas to write.`）——读仍可用。原因：Redis 复制是异步的，主库分不清"从库真死了"（继续写安全）与"只是网络断了"（继续写会脑裂丢数据），只能一刀切选保守。**停新机做演练/维护前，先执行 `CONFIG SET min-replicas-to-write 0`，恢复后设回 1**（"演练开关"）。**这是 2 台机器下 `min-replicas-to-write` 的固有局限**（该参数的设计前提是 ≥2 个从库），真正的解法是第三个副本放第三台机器——已评估，成本不划算，故接受。完整推导/拓扑对照/面试话术见 [[TODO第三批实现与原理]] §6.9。

**未来演进预判**：
- **短期（演示阶段）**：不会引入 Redis 事务，现有"单命令原子 + 锁 + 补偿"足够
- **中期**：若秒杀需"扣库存 + 标记 + 随机码"多 key 强原子，优先使用 **Lua 脚本**（原子性、可回滚语义、单次网络往返均优于 MULTI/EXEC），而非传统事务；抢券/抽奖/资格预占等"读-改-写"场景可用 `WATCH` 乐观锁
- **注意**：若未来引入 Redis Cluster，MULTI/EXEC 与 Lua 仅支持**同 slot 的 key**，跨 key 事务/脚本受限
- **与哨兵模式的关系**：切换瞬间连接中断，任何进行中的操作（含事务/多命令/脚本）都会报错，客户端重连重试即可（非 Lua 特有）；Lua **短脚本（毫秒级）与哨兵可安全共存**，大厂（阿里云 Tair / AWS ElastiCache）普遍同时使用；仅需防范"长脚本阻塞主节点导致哨兵误判"，方案见 `docs/评估报告/TODO文件.md` #10 与 `docs/归档/Redis配置加固与哨兵模式方案.md`

---

## 七、前端项目信息

> **前端项目位置**（2026-08-22 补充）：
> - **本地路径**：`D:\Vue-Workspace\csmall-vue`（Vue 3 + Vite + Element Plus 2.13.6 + Pinia）
> - **远程仓库**：https://github.com/yunxuan4309/csmall-vue.git（默认分支 master）
> - **服务器部署**：`/data/csmall/frontend/`（dist 构建产物，由 `csmall-frontend` 容器 Nginx 服务）
> - **前端文档**：前端仓库根目录 `CLAUDE.md` / `docs/前端上下文文档.md`（前端专属上下文，与本文档互补）

### 7.1 项目结构

```
csmall-vue/
├── src/
│   ├── api/                    # API 接口封装
│   │   ├── request.js          # Axios 实例（6个微服务实例）
│   │   ├── seckill.js          # 秒杀 API
│   │   ├── order.js            # 订单 API
│   │   ├── ai.js               # AI 智能导购 API
│   │   └── ...
│   ├── views/
│   │   ├── front/              # 前台页面
│   │   │   ├── home/           # 首页
│   │   │   ├── seckill/        # 秒杀列表/详情
│   │   │   ├── order/          # 订单结算/支付
│   │   │   ├── product/        # 商品展示/详情
│   │   │   └── cart/           # 购物车
│   │   └── admin/              # 后台管理
│   │       ├── login/          # 管理员登录
│   │       ├── dashboard/      # 数据仪表盘
│   │       ├── product/        # 商品管理（品牌/分类/SPU）
│   │       ├── order/          # 订单管理
│   │       └── ...
│   ├── store/                  # Pinia 状态管理
│   │   ├── user.js             # 用户状态 (Token/用户信息)
│   │   ├── cart.js             # 购物车状态
│   │   └── permission.js       # 权限状态
│   ├── router/                 # Vue Router 4 配置
│   ├── composables/            # 组合式函数
│   └── main.js                 # 入口文件
├── .env                        # 通用环境变量
├── .env.development            # 开发环境变量（6 个微服务地址）
├── .env.production             # 生产环境变量
├── vite.config.js              # Vite 配置（含代理配置）
└── package.json
```

### 7.2 前后端接口对接方式

- **开发环境**: Vite 代理将前端请求按路径分发到后端 Gateway (10087)，Gateway 再路由到各微服务
- **请求封装**: `src/api/request.js` 使用 6 个独立的 Axios 实例，分别对应 SSO/Gateway/UMS/Front/Seckill/Admin 服务
- **Base URL**: 从 `.env.development` 读取 `VITE_API_*` 环境变量
- **Token 机制**: 登录后 JWT 存入 `localStorage.getItem('mall_token')`，请求拦截器自动添加 `Bearer` 前缀
- **响应格式**: 后端统一返回 `{ state: 20000, message: "ok", data: {...} }`（注意：不同服务可能使用 `20000` 或 `200`）
- **History 模式**: 路由使用 `createWebHistory`，需 Nginx 配置 `try_files $uri $uri/ /index.html` 回退

### 7.3 前端关键注意事项

1. Element Plus 2.13.6 中 `label` 已弃用，改用 `value`
2. 空字符串可能触发后端校验，可选字段应传 `null`
3. 分页字段：不同服务可能使用 `list` 或 `records`，注意兼容
4. `request.js` 中 6 个 Axios 实例独立配置：生产环境 `.env.production` 已将 6 个 `VITE_API_*` 统一指向同一服务器地址（已修复，2026-07-26），由 Nginx 按路径前缀分发到 Gateway
5. Vue 文件统一 `<script setup>` + Composition API
6. `@` 别名指向 `src` 目录

---

## 八、重点踩坑记录与问题解决方案

> **以下内容从多份历史文档中提取整合，是项目最宝贵的经验沉淀，不可丢弃！**

### 8.1 Spring Boot 2.x → 3.x 技术栈迁移

#### javax → jakarta 包名变更

Spring Boot 3.x 基于 Jakarta EE 9+，所有 `javax.*` 包名改为 `jakarta.*`。涉及 8 个模块的 SSOFilter、Security 配置、Controller 等 25+ 文件:

- `javax.servlet.*` → `jakarta.servlet.*`
- `javax.annotation.PostConstruct` → `jakarta.annotation.PostConstruct`
- `javax.validation.*` → `jakarta.validation.*`

#### Spring Security 6.x 配置重构

`WebSecurityConfigurerAdapter` 已移除，改为 `SecurityFilterChain` Bean + Lambda DSL：

```java
// 旧（5.x）
http.csrf().disable();
http.authorizeRequests().antMatchers("/public/**").permitAll();

// 新（6.x）
http.csrf(csrf -> csrf.disable());
http.authorizeHttpRequests(auth -> auth.requestMatchers("/public/**").permitAll());
```

关键变更：
- `@EnableGlobalMethodSecurity` → `@EnableMethodSecurity`
- `setAllowedOrigins("*")` → `setAllowedOriginPatterns("*")`
- `http.sessionManagement().sessionCreationPolicy(...)` → `http.sessionManagement(session -> session.sessionCreationPolicy(...))`

#### Gateway + Knife4j 循环依赖

`Knife4jSwaggerProvider` 使用 `@Autowired` 直接注入 `RouteLocator`，形成循环依赖。解决方案：使用 `ObjectProvider<RouteLocator>` 构造器注入 + `getIfAvailable()` 延迟加载。

### 8.2 Seata 1.x → Apache Seata 2.1.0 迁移

- **groupId 变更**: `io.seata` → `org.apache.seata`
- **关键 Bug 修复**: `dubbo-filter-seata:1.0.2` 存在 `branchType is null` NPE 问题（GitHub Issue #6815）。在消费者端无条件调用 `RootContext.getBranchType()`，当不在事务中时返回 null 导致崩溃。

**自定义 Filter 解决方案**（在 mall-common 中实现）:
```java
// SeataTransactionConsumerFilter: 先检查 xid，再安全获取 branchType
String xid = RootContext.getXID();
if (xid != null) {
    invocation.setAttachment("TX_XID", xid);
    BranchType branchType = RootContext.getBranchType();
    if (branchType != null) {
        invocation.setAttachment("TX_BRANCH_TYPE", branchType.name());
    }
}
```
通过 Dubbo SPI 注册（`META-INF/dubbo/org.apache.dubbo.rpc.Filter`）。

### 8.3 JJWT 版本升级（0.9.x → 0.12.6）

**原因**: JDK 11+ 移除 `javax.xml.bind`，JJWT 0.9.x 不兼容。

**Breaking Changes**:
| 0.9.x | 0.12.6 |
|--------|--------|
| `setClaims()` | `.claims()` |
| `signWith(key, algo)` | `.signWith(key, Jwts.SIG.HS512)` |
| `Jwts.parser().setSigningKey().parseClaimsJws().getBody()` | `Jwts.parser().verifyWith(key).build().parseSignedClaims(token).getPayload()` |
| 单个 `jjwt` 依赖 | 三个：`jjwt-api` + `jjwt-impl`(runtime) + `jjwt-jackson`(runtime) |

**密钥长度问题**: JJWT 0.12.x 严格校验 HMAC 密钥长度，HS512 要求 ≥ 64 字节。全部 9 个模块的 `jwt.secret` 替换为 69 字节新密钥。

**Token 前导空格问题**: `authHeader.substring(jwtTokenHead.length())` 截取 `"Bearer"` 后得到 `" eyJ..."`（前导空格），JJWT 0.12.x 严格校验导致解析失败。在所有 `substring(tokenHead.length())` 后加 `.trim()`，共 12 处。

### 8.4 商品模块实体类与数据库字段映射问题

**实体类缺少 @TableName**: MyBatis-Plus 默认用类名转蛇形作为表名，如 `Spu` → `spu`，但实际表名是 `pms_spu`。为 25 个实体类添加 `@TableName` 注解。

**is_ 前缀字段不匹配**: MyBatis-Plus 将 `deleted` 映射为列 `deleted`，但数据库实际列名是 `is_deleted`。为 Spu 中 5 个字段、Category 中 2 个字段添加 `@TableField` 注解。

**pms_category 表结构不一致**: Mapper XML 期望的字段（`depth`、`keywords`、`enable`）与数据库实际字段（`level`、`is_parent`、`is_display`）不匹配。

**SPU 测试数据不可见**: `init-test-data.sql` 中 SPU INSERT 未显式设置 `is_checked` 和 `is_deleted`，默认 0 导致前端查询不到商品。解决：显式设置 `is_checked=1, is_deleted=0`，并将数据从 5 条扩充到 20 条。

**未修复的遗留差异**（当前使用自定义 Mapper XML，暂不影响）：`Brand`、`Attribute`、`AttributeTemplate`、`Picture`、`SkuSpecification`、`SpuDetail` 等实体类与数据库字段存在差异，若未来使用 `BaseMapper` 方法操作需修复。

### 8.5 MyBatis-Plus 版本兼容问题

- **Starter 选择**: Spring Boot 3.x 必须用 `mybatis-plus-spring-boot3-starter`，不能用 `mybatis-plus-boot-starter`
- **分页插件 jsqlparser**: 需额外添加 `mybatis-plus-jsqlparser` 依赖
- **分页类型转换**: `IPage<Model>` 无法直接转 `IPage<VO>`，需手动 stream + `convertToVO` + new `Page<>`
- **API 变更**: `mapper.update(entity)` → `mapper.updateById(entity)`；`selectCount()` 返回 `long` 不是 `int`；代码生成器 `AutoGenerator` → `FastAutoGenerator`
- **自定义 XML 插入**: 不会经过 MyBatis-Plus `ASSIGN_ID` 逻辑，需手动 `IdWorker.getId()`

### 8.6 秒杀服务问题

**布隆过滤器不可用**: 本地 Redis 3.2 (Windows) 不支持 RedisBloom 模块 → 用 Redis Set 替代（`SADD`/`SISMEMBER`），代码留 TODO 标记，生产环境需切换。

**秒杀订单创建缓慢**: 三层问题叠加 — success 表字段不匹配 → 插入失败 → RabbitMQ `basicNack(requeue=true)` 无限重试 → `logging.level: trace` 日志刷屏 → 修复表结构 + 异常抛 RuntimeException + 日志级别改为 info。

**库存超卖**: SQL `WHERE sku_id=#{skuId}` → `WHERE sku_id=#{skuId} AND seckill_stock >= #{quantity}`，消费者检查返回值。

**success 表 id 不能为空**: 自定义 XML INSERT 不经过 MyBatis-Plus ASSIGN_ID，需手动 `IdWorker.getId()`。

**其他修复**: Redis 缓存 TTL 从 5 分钟改 2 小时 5 分钟；RabbitMQ 消息从 Java 序列化改 JSON 序列化；添加 Seata `@GlobalTransactional` 和 Sentinel 流控规则。

### 8.7 RabbitMQ Windows 环境问题

**根本原因**: Windows 计算机名含中文字符导致 Erlang 节点名乱码。`rabbitmqctl` 无法连接、AMQP 5672 不监听、管理界面 15672 不可访问。

**解决步骤**:
1. 修改计算机名为纯英文 → 重启
2. 彻底卸载 RabbitMQ + Erlang，清理残留数据（`AppData\Roaming\RabbitMQ`、`.erlang.cookie`）
3. 设置环境变量 `RABBITMQ_NODENAME=rabbit@localhost`
4. 重新安装 → 启用管理插件 → 验证

**经验**: `rabbit@nohost` 是 Erlang 无法解析主机名的典型标志；`RABBITMQ_NODENAME=rabbit@localhost` 是最稳妥方案。

### 8.8 mall-resource 模块问题

**503 路由问题**: mall-resource 无 Nacos 发现依赖，未注册到 Nacos，Gateway 的 `lb://mall-resource` 路由找不到目标。解决：改为 `http://127.0.0.1:9060` 直连。

**编译错误**: 添加上传记录功能时需引入 MySQL/MyBatis-Plus/HikariCP 依赖，注意 `mybatis-plus-jsqlparser` 不可遗漏。

**Druid 连接池 BUG**: 原使用 Druid 连接池，上传请求时可能线程挂死。解决：改为 HikariCP（Spring Boot 默认）。

**Nginx 上传代理**: 需添加 `/upload/` location 块并设置 `client_max_body_size 10m`，注意 Nginx 语法不能用嵌套 location。

**SQL 密码特殊字符**: Bash 中执行含 `!` 的密码时需用单引号包裹，防止 Bash 历史扩展。

### 8.9 Maven 多模块构建问题

- **库模块跳过 repackage**: mall-common、mall-pojo、各 service 模块需配置 `spring-boot-maven-plugin` 的 `<skip>true</skip>`，避免找不到 main class
- **子模块版本管理**: 统一通过父 POM `dependencyManagement` 管理版本，子模块引用不指定 `<version>`
- **groupId 统一**: 所有模块统一为 `com.cooxiao.mall`
- **相对路径**: 所有子模块添加 `<relativePath>../pom.xml</relativePath>`

### 8.10 Gateway 路由配置差异

| 环境 | 路由方式 | 配置 |
|------|---------|------|
| 开发/测试 (test) | 手动路由 | `locator.enabled: false`，9 条完整路由（与生产对齐） |
| 生产 (prod) | 手动路由 | `locator.enabled: false`，仅暴露前台必需服务，后台管理服务不对外 |

生产环境通过 `lb://`（需要服务在 Nacos 注册）或 `http://ip:port` 直连指定路由。

### 8.11 Git 中文乱码修复

Windows 默认 GBK 编码导致中文文件名和提交日志乱码，必须配置：
```bash
git config --global core.quotepath false
git config --global i18n.commitEncoding utf-8
git config --global i18n.logOutputEncoding utf-8
git config --global gui.encoding utf-8
```
IDEA 还需：Settings → File Encodings 全部 UTF-8，VM Options 添加 `-Dfile.encoding=UTF-8`。

### 8.12 其他经验教训

- **Druid 连接池**: 线上曾因 Druid `max-wait=0` 无限等待导致上传线程挂死（2026-06，mall-resource 上传场景）。**已切换 mall-resource 为 HikariCP**；其余 6 服务仍用 Druid 稳定运行。教训：① 第三方库默认值要核实（无限等待=永久挂死）；② 普通版 `com.alibaba:druid` 不支持 `spring.datasource.druid.*` 配置（需 starter 版）；③ 文档需与线上一致（曾误记"全面替换"，2026-08-17 更正）
- **Apache Commons Lang**: `StringUtils` 包名从 `org.apache.commons.lang` 改为 `org.apache.commons.lang3`
- **Nacos RandomUtils**: 旧版 `com.alibaba.nacos.client.naming.utils.RandomUtils` 不可用，改为 `ThreadLocalRandom`
- **MediaType.APPLICATION_JSON_UTF8**: 已废弃，RFC 8259 规定 JSON 默认 UTF-8
- **CORS 配置**: `setAllowedOrigins("*")` → `setAllowedOriginPatterns("*")` + `setAllowCredentials(true)`
- **JacksonConfig**: 全局 Long→String 序列化 + 空值处理
- **Elasticsearch**: 低配服务器需配置 JVM 堆内存（`-Xms512m -Xmx512m`），避免被 OOM Killer 杀死

---

## 九、当前待办事项

> **2026-09-09 更新**：待办事项统一收口到 **`docs/评估报告/TODO文件.md`**（单一事实来源，本文件不再重复维护，避免双份漂移）。执行状态（2026-09-09）：
> - ✅ **第一批完成**（2026-09-07，#25 JWT 随机化 / R7 内存优化 / #24 MySQL 强密码 / R1~R4 Redis 加固 / #38 Dockerfile 清理）→ 明细见 `docs/评估报告/TODO已完成.md`
> - ✅ **第二批全部完成 + 已部署**（2026-09-08：#33 双索引统一 / #8 预算时区 / #36 DLX / #23 校验 / #14 P0+P1 / #5-P0 Sentinel / #2+#34 AI 限流闸门 / **#13 Nacos 认证** / **#29 数据库备份**）→ 明细见 `TODO已完成.md` + `TODO第二批实现与原理.md`
> - ✅ **第三批已完成**：#46 nacos 数据卷挂载 / #47 数据库备份恢复演练 / **#4-阶段0 定时任务分布式锁（代码+9 单测+本地双实例联调）** / **#4-阶段A 新机铺路**（2026-09-09）→ 见 `TODO第三批实现与原理.md`、`本地双实例锁验证报告-2026-09-09.md`
> - ✅ **跨机集群主线完成（2026-09-09 晚，阶段 0~5 全部）**：**#9 Redis 主从 + 3 哨兵**（故障转移演练：选主 6.1s / 客户端 9.1s 自愈 / 数据零丢失）+ **#4 秒杀双实例**（Nacos 2 实例 / 负载分布 30:30 / 锁互斥 / 停实例剔除通过）→ 归档 `TODO已完成.md` §十三，原理见 `TODO第三批实现与原理.md` §五/§六
> - ✅ **#14-P2 已完成（运行时 2026-09-09 + conf 落盘 2026-09-10 复核）**：老机主库 `min-replicas-to-write 1` / `min-replicas-max-lag 10`（`redis-master.conf` 第 15~16 行）→ **跨机集群主线 100% 收口**。⚠️ 新机从库【不开】（原因见 `TODO第三批实现与原理.md` §6.9）；停新机前先 `CONFIG SET min-replicas-to-write 0`（演练开关）
> - 🟡 **第三批剩余余项**：#5-P1 热点参数限流 / #40 actuator healthcheck / #45 Dubbo 应用名规范 / #48 模拟数据 / #49 商品集群（未来）
> - 🔴 **新发现待办**：#51 容器 restart 策略（老机重启后 20/21 容器不自动恢复，P2）/ #53 网关重试或优雅下线（停实例 16s 失败窗口，P2）/ #50 哨兵认证（观察项）/ #52 镜像获取路径（已记录事实）
> - ⏸️ 暂缓项（#10/#11/#12/#15/#16/#22/#26/#27/#30/#35/#39/#41/#42 等）→ 见 TODO 对应区块（面试讲认知，不实现）
> - ⏸️ 暂缓项（#10/#11/#12/#15/#16/#22/#26/#27/#30/#35/#39/#41/#42 等）→ 见 TODO 对应区块（面试讲认知，不实现）

---

## 附录 A：历史生产环境概览

> 项目现部署于阿里云 ECS（成都），使用 Docker 容器化部署。

### A.1 服务器信息

> **2026-09-09 起为两台服务器**（详见 [[阿里云ECS服务器情况]]）：老机 = 主环境（21 容器全栈）；新机 = 跨机集群实验（Redis 从+3哨兵 / 秒杀副本 / 造数执行位）。两台**同 VPC 同安全组**，内网互通 0.43ms。

**老机（主环境）**

| 配置项 | 值 |
|--------|-----|
| 云厂商 | 阿里云 |
| 实例名 / ID | launch-advisor-20260729 / i-2vc8cadlyv9w8dwpmivb |
| 规格 | 4核16G 经济型e (ecs.e-c1m4.xlarge) |
| 地域 | 西南1（成都）可用区B |
| 操作系统 | Ubuntu 24.04 64位 |
| 公网 IP | 8.156.77.197 |
| 私网 IP | 172.29.193.239 |
| 主机名 | iZ2vc8cadlyv9w8dwpmivbZ |
| 登录用户 | ecs-user |
| 系统盘 | ESSD Entry 100GB |
| 带宽 | 5 Mbps 固定带宽 |
| 域名 | 无（暂用 IP 访问） |

**新机（跨机集群实验，2026-09-09 创建）**

| 配置项 | 值 |
|--------|-----|
| 实例名 / ID | csmall-node2 / i-2vcg7pnk8j6mrwy56jny |
| 规格 | 2核8G 经济型e (ecs.e-c1m4.large)，¥71.28/月 |
| 地域 / 网络 | 西南1（成都）可用区B；VPC `vpc-2vcxszplmcrzxluvgdnyr` + 交换机 `vsw-2vcsykcg55qvk4sibip6y` |
| 操作系统 | Ubuntu 24.04.4 LTS（内核 6.8.0-138） |
| 公网 / 私网 IP | 47.109.70.197 / 172.29.193.240 |
| 主机名 | iZ2vcg7pnk8j6mrwy56jnyZ |
| 带宽 | 按使用流量（5 Mbps 峰值）——集群走内网，公网仅 SSH |
| 安全组 | `sg-2vc9i0zxt8gbq18mo8ea`（老机同组 → 内网互信） |
| 系统盘 | ESSD Entry 40GB |
| 已装环境 | Docker 29.8.0 + Compose v5.5.1；ai-deepseek（docker 组） |
| 到期 | 2026-10-09（**需续费**） |
| 用途 | Redis 从 + 3 哨兵 / 秒杀副本(10017) / 造数·压测执行位 |

### A.2 服务器目录结构（Docker 部署）

| 目录 | 用途 |
|------|------|
| `/data/csmall/` | 项目根目录（docker-compose.yml、.env、jars/、frontend/、dockerfiles/、setup-server.sh） |
| `/data/csmall/jars/` | 11 个微服务 JAR 包 |
| `/data/csmall/frontend/` | 前端构建产物（dist/） |
| `/data/csmall-upload/` | 用户上传文件存储 |
| Docker 数据卷 | `/var/lib/docker/volumes/`（mysql_data、redis_data、es_data、rabbitmq_data、**nacos_data**） |

### A.3 Docker 中间件配置

| 中间件 | 容器名 | 端口 | 账号/密码 |
|--------|--------|------|-----------|
| MySQL 8.0 | csmall-mysql | 3306（**2026-09-09 起绑私网 `172.29.193.239`**） | root / 强密码（见 .env，2026-09-07 轮换） |
| Redis 7 | csmall-redis | 6379（**2026-09-09 起绑私网 `172.29.193.239`**） | requirepass（见 .env，2026-09-07 加固：AOF + maxmemory 256mb） |
| Nacos 2.5.2 | csmall-nacos | 8848/9848 | 管理员密码见 .env（✅ 2026-09-08 开认证 #13；✅ 2026-09-09 数据卷 `csmall_nacos_data` #46） |
| RabbitMQ 4 | csmall-rabbitmq | 5672/15672 | 见 .env |
| Elasticsearch 8.6 | csmall-es | 9200/9300 | 无密码 |
| Seata 2.1.0 | csmall-seata | 8091/7091 | seata/seata |
| Sentinel 1.8.6 | csmall-sentinel | 8090（容器内 8858） | sentinel/sentinel |
| SkyWalking OAP 9.7.0 | csmall-skywalking-oap | 11800/12800 | ES 存储 |
| SkyWalking UI 9.7.0 | csmall-skywalking-ui | 8088 | 链路追踪看板 |

> **2026-09-07 加固记录**：Redis（requirepass + AOF everysec + maxmemory 256mb volatile-lru + 自定义 conf 挂载）、MySQL（43 位强密码 ALTER USER 双 host）、端口 3306/6379 收窄 127.0.0.1、JWT_SECRET 换 64 字符随机密钥、全容器 mem_limit + Swap 2G——详见 [[运维手册--密钥密码轮换]] 与 [[TODO第一批实现与原理]]。

> **2026-09-09 跨机集群（#9/#4）**：新机 `csmall-node2` 上跑 **5 个容器**——`csmall-redis-replica`（6380，`replicaof 172.29.193.239 6379` + `replica-announce-ip 172.29.193.240`）、`csmall-sentinel-1/2/3`（26379-26381，`sentinel monitor mymaster 172.29.193.239 6379 2`）、`csmall-seckill-2`（10017，秒杀副本）。**老机 11 个服务的 Redis 客户端已全部迁哨兵模式**（`SPRING_DATA_REDIS_SENTINEL_MASTER=mymaster` + `SPRING_DATA_REDIS_SENTINEL_NODES=172.29.193.240:26379,26380,26381`）。⚠️ 哨兵 conf 会被运行时重写（`config-epoch`/`myid`/known-*）→ **仓库模板不得反向覆盖**；`replica-announce-ip` 是"切换后能连回来"的关键。

### A.4 关键环境变量（.env 文件）

| 环境变量 | 用途 |
|----------|------|
| `MYSQL_ROOT_PASSWORD` | MySQL root 密码（2026-09-07 强随机化） |
| `REDIS_PASSWORD` | Redis requirepass（2026-09-07 启用） |
| `RABBITMQ_USERNAME/PASSWORD` | RabbitMQ 认证 |
| `JWT_SECRET` | JWT 签名密钥（2026-09-07 随机化，64 字符） |
| `AI_API_KEY` / `DEEPSEEK_API_KEY` | DeepSeek API Key（嵌套占位符双兼容） |
| `EMBEDDING_API_KEY` | 硅基流动 Embedding Key（⚠️ #44：旧 key 已泄露待吊销，勿硬编码进 yml） |
| `RESOURCE_HOST` | 文件资源访问 URL（如 http://8.156.77.197/） |

### A.5 安全组配置

> **两台共用同一安全组** `sg-2vc9i0zxt8gbq18mo8ea`（2026-09-09 起）→ **同组内网互信**：VPC 内网（172.29.193.0/20）互通不限速（实测 ping 0.43ms），公网侧按下表放行。

| 端口 | 协议 | 来源 | 用途 |
|------|------|------|------|
| 22 | TCP | 0.0.0.0/0 | SSH 远程登录 |
| 80 | TCP | 0.0.0.0/0 | HTTP 前端访问 |
| 443 | TCP | 0.0.0.0/0 | HTTPS（暂未使用） |

**⭐ 边界认知（2026-09-09 修正，重要）**：**安全组是唯一的公网边界**。阿里云公网 IP 是 **NAT 映射、不在网卡上** → 发往公网 IP 的包会被 DNAT 成私网 IP 再进网卡，因此服务绑 `0.0.0.0` 还是绑私网 IP，**公网可达性完全等价**（实测 `6379/3306/10087/26379/6380` 全 blocked、仅 `:80` 通，与绑法无关）。绑定地址只决定"本地哪些地址接受连接"，属**纵深防御**而非边界。分层模型：**SG（边界）→ bind/ports（监听面）→ 认证（身份）→ TLS（传输）**，详见 [[TODO第三批实现与原理]] §5.6⑥。

**跨机内网实测（2026-09-09，新机 → 老机）**：22/80/8848/5672/8091/9200/10007 ✅ 通；3306（MySQL）/ 6379（Redis）初测 ❌ 不通——第一批安全加固把这两个端口绑到 `127.0.0.1`（见 A.3 加固记录），**这是跨机集群的硬前置**；**阶段 2 已改绑宿主私网 IP `172.29.193.239`**（不是 `0.0.0.0`，作纵深防御）且**实测无需改安全组**（同组内网互通不受公网规则限制），详见 TODO「🚀 第三批执行清单」阶段 2。

### A.6 部署方式

- **Docker Compose**：`/data/csmall/docker-compose.yml` 编排 **21 个容器**（7 基础中间件 + 2 链路服务 + 11 微服务 + 1 前端）
- **环境变量**：`.env` 文件注入敏感配置
- **日志**：所有服务输出 stdout，通过 `docker logs` 查看
- **ES 内存**：限制 `-Xms512m -Xmx512m`（docker-compose 中配置）
- **2026-09-09 起同一份 compose 含 5 个"跨机集群成员"**（`redis-replica` / `redis-sentinel-1~3` / `mall-seckill-2`），**只在第二台机器 csmall-node2 上启动**；老机仍只启动原有 21 个。

> 🔴 **新机（csmall-node2）上永远不要裸跑 `docker compose up -d`**
>
> 两机共用同一份 `docker-compose.yml`（跨机方案 A），文件里既有老机的 21 个服务，也有新机的 5 个集群成员。**裸跑 `docker compose up -d`（不带服务名）会把老机那 21 个服务在新机上一起拉起来**——老机中间件/业务在新机上没有数据与配置，会起一堆必然失败或半残的容器，并且可能抢占端口/注册到同一 Nacos 造成实例混乱。
>
> **正确用法（必须带服务名）**：
> ```bash
> cd /data/csmall
> # 新机：只拉起自己的集群成员
> docker compose up -d redis-replica redis-sentinel-1 redis-sentinel-2 redis-sentinel-3
> docker compose up -d --no-build mall-seckill-2      # 镜像已从老机 load 时用 --no-build
> # 老机：只拉起原有服务
> docker compose up -d mysql redis nacos mall-seckill ...
> ```
> 同理，排查/重启单个服务时也用 `docker compose up -d <服务名>`，不要用无参数形式。详见 [[跨机集群实施执行清单-2026-09-09]] §四·五。

> 🧾 **下发文件必须校验哈希（2026-09-09 起）**
>
> 跨机下发（compose / 配置 / jar）要经过 `本地仓库 → scp → /tmp → sudo cp → /data/csmall` **4 跳**，任一跳都可能**静默改字节**（CRLF↔LF、UTF-8 BOM、传输截断、`cp` 未报错的失败）。落地纪律：**本地算 md5 → 服务器落位后 `md5sum` 比对，一致才继续**——哈希是内容指纹而非版本号，它把"仓库为事实来源"从口号变成**可验证断言**。
>
> 原理、行业做法（ISO SHA-256 / 镜像 digest / Helm `checksum/config` / Ansible 漂移检测）与 MD5 局限（只防意外、不防篡改，安全场景用 SHA-256）见 [[跨机集群实施执行清单-2026-09-09]] §十二 与 [[TODO第三批实现与原理]] §5.8。凭据文件（`.env`）只查行数与权限，**连哈希也不对外报**。

> 🔴 **容器 restart 策略：重启后老机 20/21、新机 5/5 个容器都不会自动恢复（2026-09-09 巡检发现，2026-09-10 两机复核，TODO #51）**
>
> **老机** 21 个容器实测：**14 个 `no` + 6 个 `on-failure` + 1 个 `unless-stopped`**；**新机** 5 个容器**全部 `on-failure`**。
> **关键认知（2026-09-10 经 [Docker 官方文档](https://docs.docker.com/engine/containers/start-containers-automatically/) 核实）**：*"`on-failure` only prompts a restart if the container exits with a failure. **It doesn't restart the container if the daemon restarts**."* → **只有 `unless-stopped` 能扛住宿主/daemon 重启**：老机只有 `csmall-resource` 会自己回来（20/21 不会），**新机 5/5 全不会回来**。两台 `LiveRestoreEnabled=false`、docker 均 `enabled`（开机自启 daemon），且**无 systemd 单元 / cron 兜底**。
> ⚠️ **新机不恢复的连锁后果**：Redis 从库 + 3 哨兵同时消失 → 老机 Redis 主库因 `min-replicas-to-write 1` **拒绝所有写**（`NOREPLICAS`）→ 秒杀/登录等写入路径全挂，且哨兵全灭无法故障转移。
> 老机已连续运行 42 天（截至 2026-09-10），所以从未暴露。
>
> **因此**：① 不要在无窗口的情况下重启任一机器的 Docker（会停全站）；② 恢复方式 = 逐个 `docker compose up -d <服务名>`（**永远不要裸跑 `up -d`**，见上方红线）；③ 根治方案（TODO #51）：compose 全量改 `restart: unless-stopped`（**两台都要改**：老机 20 个 + 新机 5 个）并逐个 recreate。

### A.7 关键文件路径

| 配置 | 路径 |
|------|------|
| Docker 部署 | `deploy/docker/` |
| 环境变量模板 | `deploy/docker/.env.example` |
| Gateway 生产配置 | `mall-gateway-server/.../application-prod.yml` |
| 前端 Nginx 配置 | `csmall-vue/nginx.conf` |

---

## 附录 B：文档索引

本文件整合了以下原始文档的关键信息：

- `docs/归档/AI智能导购模块开发计划.md` — AI 模块四阶段开发计划（📦 2026-09-10 归档：计划已 100% 完成、清单已漂移）
- `docs/归档/Docker部署审计报告.md` — Docker 容器化部署审计（📦 2026-09-10 归档：一次性基线快照；归档时已加「现状对照表」，8 项发现 5 项已解决/1 项失效/2 项转 TODO #40·#44·#22）
- `docs/评估报告/TODO文件.md` — 待办事项和详细方案
- `docs/归档/模块启动顺序.md` — 启动顺序和依赖关系图（已归档，内容已收入 §3.5）
- `docs/问题解决/问题解决--SpringBoot2升级3.md` — SB 2.x→3.x 迁移记录
- `docs/问题解决/问题解决--JDK21与Seata迁移.md` — JDK 21/Seata 2.1 迁移记录
- `docs/问题解决/问题解决--Maven多模块构建.md` — Maven 多模块问题
- `docs/问题解决/问题解决--商品实体映射.md` — 实体类/数据库映射问题
- `docs/问题解决/问题解决--登录.md` — 登录 Bug 修复记录
- `docs/问题解决/问题解决--RequestBody缺失.md` — 参数绑定问题
- `docs/问题解决/问题解决--RabbitMQ启动.md` — RabbitMQ Windows 问题
- `docs/问题解决/问题解决--秒杀.md` — 秒杀模块问题
- `docs/问题解决/问题解决--秒杀购物车订单.md` — 秒杀/订单修改
- `docs/问题解决/问题解决--首次部署问题分析.md` — 生产部署问题分析
- `docs/问题解决/问题解决--生产环境部署.md` — systemd/ES 问题
- `docs/问题解决/问题解决--后端部署配置.md` — 部署详细方案
- `docs/问题解决/问题解决--资源服务.md` — resource 模块问题
- `docs/问题解决/问题解决--安卓图片上传.md` — 图片上传问题
- `mall-ai/AI模块问题与解决方案总结.md` — AI 模块问题总结
- `mall-leaf/DEPRECATED.md` — Leaf 弃用说明
- `database/README.md` — 数据库初始化说明
- 前端 `README.md` / `docs/前端上下文文档.md` — 前端项目文档
- `docs/问题解决/问题解决--AI导购模块部署.md` — AI 模块部署（CORS/API Key/Session/Nacos冲突）
- `docs/问题解决/问题解决--前端体验优化、订单与秒杀功能修复.md` — 前端体验与后端缺陷修复
- `docs/问题解决/问题解决--SkyWalking部署与兼容性修复.md` — SkyWalking 部署（Alpine→Debian/Metaspace/Seata）
- `docs/问题解决/问题解决--Dubbo超时与ES同步修复.md` — Dubbo 超时 + ES 索引修复
- `docs/评估报告/JVM调优方案.md` — JVM 三档调优（G1GC/分堆/Metaspace）
- `docs/运维/运维速查--SSH隧道与访问地址.md` — SSH 隧道访问后台管理工具
- `docs/问题解决/问题解决--秒杀购买标记重构与后台管理全面修复.md` — 秒杀三 key 分离 + 管理员后台 SPA/权限/仪表盘/分类修复
- `docs/问题解决/问题解决--管理员登录BCrypt兼容性.md` — 管理员登录 BCrypt 兼容性问题
- `docs/问题解决/问题解决--resource容器Nacos竞态与restart缺失.md` — resource 容器 Nacos 注册竞态 + restart 缺失修复

> 🧩 **2026-09-10 新增：8 个「一类问题」聚合文档**（由 `TODO第一批/第二批实现与原理` 的技术正文**切割**而来；设计原则 = **不按"一个问题一篇"，而按"一类问题的本质"聚合**，每篇统一结构：**这一类问题的共性 → 逐问题（现象→排查→根因→修复→验证）→ 跨问题踩坑 → 面试综述话术**）：
> - `docs/问题解决/问题解决--生产安全加固与凭据治理.md` — 凭据/认证"看着配了、实际等于没有"（#25 JWT / #24 MySQL / #13 Nacos）
> - `docs/问题解决/问题解决--Redis加固与主从数据一致性.md` — 异步复制中间件如何不丢数据、不失真（R1~R4 / #14 / #14-P2）
> - `docs/问题解决/问题解决--资源治理与流量防护.md` — 资源与流量的硬边界（R7 内存 / #5-P0 Sentinel / #2+#34 闸门）
> - `docs/问题解决/问题解决--服务注册与网关路由.md` — 一个服务多个身份 → 路由打错端口（#6 Dubbo 撞名）
> - `docs/问题解决/问题解决--消息可靠性与毒消息治理.md` — 消息不丢、不毒、不无限重试（#36 / #14 §4.2）
> - `docs/问题解决/问题解决--请求校验与静默失效.md` — 不报错 ≠ 生效（#8 时区 / #23 校验+异常）
> - `docs/问题解决/问题解决--搜索双索引与降级分层.md` — 架构债 + 降级分层：慢≠挂、进程级降级（#33）
> - `docs/问题解决/问题解决--容器构建与编排卫生.md` — 单一事实源 + 容器编排隐含约束（#38 / §9.3 / #51）
- `docs/归档/企业级商品管理升级--测试建议.md` — 企业级商品升级测试建议（模板/SKU/换模板保护，已归档）
- `docs/Redis使用避坑指南.md` — Redis 使用常见错误清单（20 条分类）+ 项目现状对照 + 上线自查清单（2026-08-21 新增）
- `docs/归档/Redis集群适配与配置管理评估.md` — Redis 集群扩容代码适配性（含 MOVED/ASK 重定向）、Nacos 配置中心必要性、jar 多副本必要性三份评估（2026-08-21 新增）
- `docs/归档/Kubernetes与ZooKeeper评估.md` — K8s 与 ZooKeeper 使用评估（实测确认未使用）+ 误导点排查 + 面试知识要点（2026-08-22 新增）
- `docs/评估报告/数据库Schema漂移审计.md` — 实体类/数据库/建表脚本三方对比审计（2026-08-04）
- `docs/归档/服务器巡检与待修复问题清单-2026-08-04.md` — 巡检问题清单与修复记录
- `docs/归档/企业级生产评估报告-2026-08-15.md` — 企业级生产基线评估快照（P0/P1 待办已入 TODO）
- `docs/归档/Redis配置加固与哨兵模式方案.md` — Redis 加固 + 哨兵模式方案（✅ 加固部分 2026-09-07 执行；✅ 哨兵模式 #9 已于 2026-09-09 完成）
- `docs/归档/服务器内存优化方案.md` — 服务器内存优化方案（✅ 2026-09-07 已执行 R7：mem_limit + Nacos 降堆 + Swap）
- `docs/评估报告/TODO已完成.md` — 已完成任务归档（第一批/第二批/历史，单一事实来源之一）
- `docs/评估报告/TODO第一批实现与原理.md` / `TODO第二批实现与原理.md` — 各批次"原理→实现→踩坑→面试话术"
- `docs/归档/第二批部署执行清单-2026-09-08.md` — 第二批部署执行 + 验证回填清单
- `docs/归档/搜索双索引统一与一致性评估.md` — #33 A1/A2 方案全文与复核证据
- `docs/归档/秒杀对账任务实现方案.md` — #14 P1 对账任务方案
- `docs/运维/运维手册--密钥密码轮换.md` — 密钥/密码轮换操作手册（2026-09-07 用）
- `docs/归档/` — 已执行/过时文档（本次部署指南、企业级升级计划等）

**说明**: 原始文档仍保留在各目录中，本文件提供整合后的全局视图。如需查看特定问题的完整细节，可参阅对应的原始文档。

---

> **维护提示**: 此文档是项目的"单一事实来源"，任何架构变更、新增模块、重要问题修复都应及时更新本文档的相关章节。
