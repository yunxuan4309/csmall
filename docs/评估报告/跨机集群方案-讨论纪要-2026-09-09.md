# 跨机集群方案 — 讨论纪要（2026-09-09）

> **状态**: 📋 讨论定稿待拍板（新机采购未下单、拓扑待确认、未开始实施）
> **参与者**: 用户（项目 owner）+ AI（deepseek）
> **目的**: 记录 2026-09-09 关于"新服务器 + Redis 主从哨兵 + 秒杀双实例集群"的完整讨论结论，供用户仔细审阅后再决定是否推进
> **关联**: [[TODO文件]] #4/#9/#14-P2/#15/#42、[[TODO第三批实现与原理]]、[[Redis配置加固与哨兵模式方案]]、[[集群化与配置中心迁移方案]]、[[Kubernetes与ZooKeeper评估]]

---

## 一、讨论背景

用户担心老机（4C16G，available 2.9G + Swap 2G）继续叠加集群成员会 **OOM**，提出：
1. 是否开一台新小服务器，把 k3s + Redis 主从一起做
2. 秒杀集群（2 实例）与 Redis 集群（2 副本）的**管理方式**担忧
3. 模拟数据方案（已收口为 TODO #48，本次不展开）

---

## 二、核心结论速览

| # | 结论 | 说明 |
|---|------|------|
| 1 | **拓扑修正：Redis = 1 主 + 1 从 + 3 哨兵**（不是"2 副本"） | 数据节点主从不需奇数；**哨兵必须奇数（3 个，quorum=2）**才能多数派投票 |
| 2 | **秒杀 = 老机 10007 + 新机 10017 双实例**（用户已确认） | 注册老机 Nacos，gateway `lb://` 跨机轮询 |
| 3 | **k3s 对这个规模"现在不值"（不是没用）** | 2 个跨机成员恰在 k3s 最不擅长的象限；见 §四 |
| 4 | **管理方式 = 方案 A：一套 compose 两机各拉起自己的服务** | 用户已选定；见 §五 |
| 5 | **新机规格推荐：2C8G 同地域同 VPC**（成都） | 见 §六 |
| 6 | **前置硬障碍：MySQL/Redis 绑 127.0.0.1 需放行 + 注册 IP 配置** | 第一批加固的副作用，跨机必改；见 §七 |
| 7 | **模拟数据已入 TODO #48**（含 100 req/s + 每日高峰需求记录） | 本次不实施 |

---

## 三、目标拓扑（2026-09-09 讨论版，待确认）

```
                    老机 8.156.77.197（4C16G，演示主环境）
┌──────────────────────────────────────────────────────────────┐
│  MySQL 主（cs_mall_* 6 库，数据唯一真源）                      │
│  Redis 主（6379）── 现有生产数据                              │
│  Nacos / RabbitMQ / ES / Seata / Sentinel / SkyWalking       │
│  11 微服务 + gateway + 前端（不变）                            │
│  mall-seckill 实例1（10007）                                  │
└──────────────────────────────────────────────────────────────┘
                    │ 同 VPC 内网（172.x，内网互通免费不限速）
                    ▼
                    新机 2C8G（成都同可用区/同 VPC）
┌──────────────────────────────────────────────────────────────┐
│  Redis 从（6380，replicaof 老机主，只读复制）                   │
│  哨兵 ×3（26379，奇数，quorum=2，监视老机主 + 本机从）           │
│  mall-seckill 实例2（10017，-Xmx256m）                        │
│    └─ 连老机 MySQL/Redis 主/MQ/Nacos（无独立业务数据）           │
│  （可选预留）k3s 学习区 / 压测脚本执行位                         │
└──────────────────────────────────────────────────────────────┘
数据唯一真源 = 老机 MySQL；Redis 从 = 老机主的复制副本；
秒杀副本无状态、无独立数据 → 不存在"数据放哪台"的抉择
```

---

## 四、k3s 价值边界（为什么"现在不值"，不是"没用"）

| k3s/K8s 擅长 | 本项目需求 | 匹配度 |
|---|---|---|
| 大量无状态服务统一调度/扩缩容/自愈 | 只有 1 个秒杀副本（无状态） | ❌ 1 个实例不需要调度器 |
| 有状态应用编排 | Redis 主从是 **K8s 里最麻烦的有状态应用**（StatefulSet+PV+哨兵），比裸 compose 更复杂 | ❌ 方向反了 |
| 跨机容器网络 overlay | 唯一有点用，但 2 个成员手动配 IP 即可 | ⚠️ 边际价值小 |
| 滚动更新/灰度 | 演示项目手动 compose up 足够 | ❌ |

**结论**：k3s 的价值在"几十个无状态服务 + 自动扩缩容/自愈"的规模化场景；本项目"2 个 Redis（有状态）+ 1 个秒杀副本（无状态）"**恰好在 k3s 最不擅长的象限**。与 TODO #42、K8s 评估文档结论一致：**k3s 不是跨机演示的前置，是后续学习/规模化的选项**。且注意：k3s 只管它自己调度的容器，老机 21 个 compose 容器不会自动进 k3s，强行引入形成"双轨"，管理复杂度反而更高。面试时"知道 k8s 何时该用、何时不该用"本身就是加分项。

---

## 五、管理方式（用户已选方案 A）

**方案 A：一套 compose 文件，两机共用，各拉起属于自己的服务**

```
老机：docker compose up -d mysql redis nacos ... mall-seckill      # 拉起主 Redis + 秒杀1 + 全部业务
新机：docker compose up -d redis-replica redis-sentinel-1 redis-sentinel-2 redis-sentinel-3 mall-seckill-2
      # 只拉起从/哨兵/副本
```

**优点**：一份文件、配置同源（Redis 密码/Nacos 地址只维护一份）、每机 `docker compose ps` 看自己的
**备选 B（不用）**：两套独立 compose（老机不动、新机单独文件）——配置要维护两份，易漂移

### 你日常真正要做的管理（极少）

```bash
# 看集群状态（三视角）
docker ps | grep -E "redis|sentinel|seckill"            # 老机+新机各跑（或脚本）
redis-cli -h 老机IP info replication                     # 主视角：role=master / connected_slaves
redis-cli -h 新机IP -p 6380 info replication             # 从视角：role=slave / master_link_status:up
redis-cli -h 新机IP -p 26379 sentinel master mymaster    # 哨兵视角：谁才是当前主

# 更新秒杀副本（发新版）
老机：docker compose up -d mall-seckill
新机：docker compose up -d mall-seckill-2

# Redis 故障演练（面试演示）
docker stop csmall-redis          # 老机主挂 → 哨兵自动提升新机从 → 服务无感
```

**核心认知**：不需要"管理集群"，需要三个**自动机制**各自就位：
1. **Nacos** = 服务发现/故障剔除（秒杀两实例谁活着、gateway 轮询到谁）——已有，自动
2. **哨兵** = Redis 故障转移（主挂→从提升，客户端重连）——需配置，故障时自动
3. **compose** = 进程拉起（每机各自）——已有

**诚实边界**：跨机 = 管理分散在两台机器（日志/ps 分两处看），这是两台机器的固有成本，k3s 只是把命令统一成 kubectl，不减少"要理解两台机器"的事实。

---

## 六、新服务器规格推荐（待拍板）

### 需求测算（实际占用）

| 负载 | 内存 | CPU |
|---|---|---|
| Redis 从（6380） | ~150M | 极低 |
| 哨兵 ×3（26379） | ~50M ×3 = 150M | 极低 |
| mall-seckill 副本（-Xmx256m） | ~500-600M（limit 768M） | 低（压测时有波峰） |
| OS + Docker + 余量 | ~1G | - |
| （可选）k3s 学习区 | +500M | 低 |
| **合计** | **~2.4G（8G 富余）** | 2 核足够 |

### 推荐规格：2C8G 经济型 e（ecs.e-c1m2.xlarge）

| 维度 | 建议 | 理由 |
|---|---|---|
| vCPU | 2 核 | 从/哨兵/秒杀副本均为低负载；压测波峰 2 核可扛（老机也只有 4 核） |
| 内存 | 8G | 实际用 ~2.4G，余量给 k3s 学习 + 压测脚本 + 未来扩展；**不选 4G**（+k3s 后仅剩 ~1G，重蹈老机覆辙） |
| 地域/VPC | **成都，同 VPC**（内网互通） | Redis 主从必须内网（跨公网=复制延迟+安全洞）；内网流量免费不限速 |
| 系统盘 | ESSD 40-60G | 从库数据 + 日志 + k3s 镜像够用 |
| 带宽 | 5Mbps（按量即可） | 集群走内网不占公网；公网仅 ssh 用 |
| 付费 | 包月/按量（与老机按月续费习惯一致） | 活动价约 ¥100-200/月量级，以官网为准 |
| 到期策略 | 可随用随停（按量）或包月 | 实验期建议按量，稳定演示再转包月 |

> ⚠️ 购买时注意：同 VPC 需要在**同一地域同一 VPC 内**（老机 VPC: vpc-2vcxszplmcrzxluvgdnyr，可用区 B）。可用区不同但同 VPC 内网通常也通，同可用区延迟最低。
> 参考：[阿里云 2核8G 选购指南](https://www.tengxunyun8.com/12886.html)、[阿里云经济型 e 规格解析](https://developer.aliyun.com/article/1695758)、[2026 阿里云价格参考](https://developer.aliyun.com/article/1708567)（价格为市场参考，以官网/活动价为准）

---

## 七、前置硬障碍（跨机必改，第一批加固的副作用）

### 7.1 MySQL(3306) / Redis(6379) 绑定 127.0.0.1（实测确认）

第一批 #24/R1 把这两个端口收窄到 `127.0.0.1`。**新机副本/从库从内网访问老机 3306/6379 会被拒绝**。

**解法**：compose 端口映射改为绑定**内网 IP**（如 `172.29.193.239:3306:3306`）或 `0.0.0.0`，并在**阿里云安全组层面仅放行两台机器互访**（不是公网 0.0.0.0/0）——纵深防御不降级：
```yaml
# docker-compose.yml
mysql:
  ports: ["172.29.193.239:3306:3306"]   # 或 0.0.0.0 + 安全组限定
redis:
  ports: ["172.29.193.239:6379:6379"]
```
> 注意：老机服务间走容器网络（container 名直连）不受端口映射影响，此改动只影响"外部（新机）访问"路径。

### 7.2 注册 IP（新机容器以容器 IP 注册 Nacos，老机 consumer 不可达）

新机副本默认以容器 IP（172.18.x）注册 → 需显式覆盖为**新机内网 IP**：
```yaml
# 新机副本 env 注入
SPRING_CLOUD_NACOS_DISCOVERY_IP: <新机内网IP>
# Dubbo 注册 IP 同理（dubbo.protocol 或 registry 参数）
```
老机 gateway `lb://mall-seckill` 轮询到新机实例后，需能路由到新机内网 IP:10017（依赖 7.1 的网络 + 新机端口映射）。

### 7.3 秒杀副本连接串全部指向老机

副本无独立数据，所有中间件地址用老机内网 IP（不能再用容器名 `mysql`/`redis`）：
```
SPRING_DATASOURCE_URL: jdbc:mysql://老机内网IP:3306/cs_mall_seckill
SPRING_DATA_REDIS_HOST: 老机内网IP
SPRING_RABBITMQ_HOST: 老机内网IP
Nacos/Sentinel 同理
```

### 7.4 秒杀多实例代码改造（唯一硬改造）

`MessageRetryTask`（每 5s 定时重发）双实例会**双跑** → 需 Redis 分布式锁（SETNX TTL）或 ShedLock（与 #4 单机集群同一改造点，跨机只是把"同机双实例"换成"两机双实例"）。

### 7.5 对账任务并发

P1 对账任务（SeckillReconcileTask）同为 @Scheduled → 也需同一把分布式锁（TODO 第二批已预告"集群化后需加锁"）。

---

## 八、风险与边界（诚实声明）

| 项 | 说明 |
|---|---|
| 跨机真 HA 的边界 | 2 台小机器防"单机宕机"（老机挂→秒杀流量切新机、Redis 主切新机从）；**不防地域灾难**；其他 19 容器仍是老机 compose 单实例 |
| Redis 主从异步复制 | 主挂瞬间最后几笔写可能未同步到从 → 靠 DB `seckill_stock>=qty` 条件扣减兜底，不会真超卖（已有 #14 P0/P1） |
| 限流语义 | Sentinel 按实例统计 → 秒杀副本后集群 QPS 翻倍（QPS=10×2），面试主动讲清（#5 P2 集群流控可后续上 token server 统一配额） |
| 网络放行的安全 | 3306/6379 从 127.0.0.1 放开 = 攻击面增大 → **必须安全组限定两台机器互访**，不能裸 0.0.0.0 |
| Seata/MQ | 多实例天然安全（undo_log 在 DB、MQ 工作队列单实例消费），无需改造 |

---

## 九、执行顺序建议（若拍板推进）

```
阶段 0：老机本地代码改造（MessageRetryTask + 对账任务分布式锁）+ 编译验证（不部署）
阶段 1：新机采购到位（2C8G 同 VPC）→ ssh 打通 → docker 安装
阶段 2：老机端口放行改造（3306/6379 绑内网 IP + 安全组限定）→ 验证内网互通
阶段 3：新机起 Redis 从 + 3 哨兵 → 验证主从复制 + 故障转移演练（kill 主 → 自动提升）
阶段 4：新机起 mall-seckill-2（10017，注册 IP 覆盖）→ Nacos 见 2 实例 → 压测验证负载均衡 + 停实例故障剔除
阶段 5：文档回填（拓扑/踩坑/面试话术入 [[TODO第三批实现与原理]]）
（可选）阶段 6：k3s 学习区（新机独立区，不混业务）
```

---

## 十、待用户拍板项

- [ ] 拓扑确认：Redis 1主+1从+3哨兵 / 秒杀 2 实例（10007 老机 + 10017 新机）—— 对吗？
- [ ] 新机是否现在采购？规格 2C8G 经济型 e 同 VPC（成都）可否？
- [ ] 管理方式 A（一套 compose 两机各拉起）确认
- [ ] 阶段 0（本地代码改造）是否先启动（不依赖新机，可立即做）？

---

**维护提示**: 本纪要随讨论推进更新；拍板后转入执行清单文档（或并入 [[TODO第三批实现与原理]] 对应章节）。
