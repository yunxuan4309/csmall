# 第二批部署执行清单（2026-09-08，维护窗口用）

> **状态**: 📋 已定稿待执行——第二批代码批（#33/#8/#36/#23/#14 P0+P1）全部完成，服务器仍运行 8-04 旧 jar，本次一次窗口全量上线。
> **前置事实**（2026-09-08 实测）：
> - 服务器 11 个微服务 jar 时间 = 2026-08-04（ams/ums/front 更早）——**落后 1 个月代码**
> - 已 commit：后端 `2b3511d`（#33 + 全部）+ 更早 20 个 commit；前端 `fa7dd8f`
> - AI 账号（ai-deepseek）**无 sudo**，写 `/data/csmall/jars/` 与 compose 操作权限见 §4
> - 关联文档：[[TODO文件]] 第二批部署状态、[[TODO第二批实现与原理]] §五/§5.4.5、[[搜索双索引统一与一致性评估]]

---

## 一、改动范围总览（本次要上线什么）

### 1.1 代码（11 模块全量重建——common/pojo 变更影响所有依赖方）

| 模块 | 重建原因 | 本次核心改动 |
|------|---------|-------------|
| mall-pojo | ✅ 依赖基础 | DTO 补校验规则（#23）、OrderAddDTO/OmsOrder 加 order_type（#14）、SearchResultVO 复用（#33）|
| mall-common | ✅ 依赖基础 | 全局异常补 MethodArgumentNotValidException/ConstraintViolationException handler（#23）|
| mall-sso | ✅ 依赖 pojo/common | 编译重建（校验链路生效）|
| mall-ams | ✅ 改代码 | AdminAddDTO 校验恢复 + updateAdmin 密码成对 bug 修复 + @Validated（#23）|
| mall-ums | ✅ 改代码 | doRegister 补 @Valid + 地址 DTO 规则（#23）|
| mall-product | ✅ 改代码 | SpuServiceImpl 补同步触发（#33）；依赖 pojo/common |
| mall-order | ✅ 改代码 | Flyway V6 order_type + DLX + OrderDlxConsumer + 支付前校验方案Y（#36/#14）|
| mall-seckill | ✅ 改代码 | SeckillQueueConsumer 三兜底 + order_type=1 + 对账任务 SeckillReconcileTask（#14）+ VO 缓存失效（#14 附带）|
| mall-search | ✅ **大改** | 只读统一索引普通搜索（#33 A2 核心）|
| mall-ai | ✅ 改代码 | 同步模型补全 + AI 重排 jsonMode + timeout 60s + key 嵌套占位符（#33/#8 + 本地修复）|
| mall-resource | ✅ 依赖 pojo/common | 编译重建 |
| mall-front | ✅ 依赖 pojo/common | 编译重建 |
| mall-gateway | ✅ 依赖 pojo/common | 编译重建（路由未改，仅跟随依赖）|

### 1.2 Flyway（order 库新增迁移）
- `mall-order-webapi/src/main/resources/db/migration/V6__add_order_type_to_oms_order.sql`（#14 治本）
- ⚠️ **部署后 order 服务首次启动会自动执行 V6**（加 order_type 列，历史数据默认 0）——需先备份 oms_order 表

### 1.3 配置/部署文件
| 文件 | 变更 |
|------|------|
| `deploy/docker/docker-compose.yml` | mall-search 服务补 `CUSTOM_FILE_UPLOAD_RESOURCE_HOST` env（#33 图片 URL）|
| mall-search `application-prod.yml` | 补 `custom.file-upload.resource-host` |
| mall-ai `application.yml` | `cooxiao.ai.timeout` 15s→60s；api-key 嵌套占位符 `${AI_API_KEY:${DEEPSEEK_API_KEY:sk-placeholder}}` |
| mall-ai `application-test.yml` | api-key 嵌套占位符（本地用，不影响 prod，随 jar 上传无害）|

### 1.4 前端（独立仓库 csmall-vue）
- `ProductList.vue`（降级 fallback）、`request.js`（silent + errType）、`search.js`（fallback API + silent）
- 部署 = 重新 `npm run build` → dist 上传 `/data/csmall/frontend/dist/`

### 1.5 服务器索引操作（#33 清理，见 §5）
- 删空索引 `cool_shark_mall_index`
- 重建 AI 索引 `/ai/syncAll`（新逻辑剔除已下架 18 → 验证 19 条）
- 删旧索引 `cool_shark_mall_index2`（mall-search 已只读 AI 索引）

---

## 二、构建（本地，AI 可执行）

### 2.1 后端全量构建
```powershell
cd D:\java\csmall
mvn clean package -DskipTests   # 全量（common/pojo 变更需 -am 全链）
```
产物：各 `*-webapi/target/*-0.0.1-SNAPSHOT.jar`（gateway 在 mall-gateway-server/target/）

### 2.2 前端构建
```powershell
cd D:\Vue-Workspace\csmall-vue
npm run build   # 产出 dist/
```

---

## 三、jar 重命名对照（mvn 产物 → 服务器短名）

| mvn 产物 | 服务器目标 |
|---------|-----------|
| mall-sso-webapi... | mall-sso.jar |
| mall-product-webapi... | mall-product.jar |
| mall-order-webapi... | mall-order.jar |
| mall-seckill-webapi... | mall-seckill.jar |
| mall-ums-webapi... | mall-ums.jar |
| mall-ams-webapi... | mall-ams.jar |
| mall-front-webapi... | mall-front.jar |
| mall-search-webapi... | mall-search.jar |
| mall-ai-webapi... | mall-ai.jar |
| mall-resource... | mall-resource.jar |
| mall-gateway-server... | mall-gateway.jar |

> ⚠️ 确认实际 artifactId（部分模块 webapi 的 jar 名可能是 `mall-xxx-webapi-0.0.1-SNAPSHOT.jar`，resource/gateway 无 -webapi 后缀，以 target 实际产物为准）。

---

## 四、服务器执行（权限分工）

> AI（ai-deepseek）：docker 操作 ✅、读 ✅、scp 到 /tmp ✅、**写 /data/csmall/jars ❌（无 sudo）**
> ecs-user（人类）：sudo 写 jars/、systemd、sudoers

### Step 1 备份（必须，ecs-user）
```bash
cd /data/csmall/jars && mkdir -p backup-20260908 && cp *.jar backup-20260908/
# Flyway 前备份 order 表
docker exec csmall-mysql mysqldump -uroot -p'<MYSQL密码>' cs_mall_oms oms_order > /data/csmall/backup/oms_order_pre_V6_20260908.sql
```

### Step 2 上传 jar（AI scp 到 /tmp → ecs-user mv）
```bash
# AI 本地执行 scp 11 个 jar 到 /tmp/<模块>.jar
# ecs-user 服务器执行：
cd /tmp
sudo mv mall-sso.jar mall-product.jar mall-order.jar mall-seckill.jar \
        mall-ums.jar mall-ams.jar mall-front.jar mall-search.jar \
        mall-ai.jar mall-resource.jar mall-gateway.jar /data/csmall/jars/
sudo chown ecs-user:ecs-user /data/csmall/jars/*.jar
```

### Step 3 同步 compose + yml（ecs-user，或 AI 若可写则 AI）
- 本地 `deploy/docker/docker-compose.yml` → 服务器 `/data/csmall/docker-compose.yml`（含 mall-search RESOURCE_HOST env）
- yml 已打进 jar（application-prod.yml），无需单独同步

### Step 4 重建容器（AI 可执行，建议分批 + 依赖顺序）
```bash
cd /data/csmall
# ① 先 order（触发 Flyway V6）——单独起，确认迁移成功
docker compose up -d --build mall-order && sleep 20 && docker logs csmall-order --tail 50
# 确认日志出现 "Migrating schema ... to version 6" 且无 ERROR
# ② 其余并行/分批（注意 product 先于依赖它的消费者）
docker compose up -d --build mall-common-deps...   # 按 compose 服务名逐个
# 实际按服务名：mall-sso mall-product mall-seckill mall-ums mall-ams mall-front mall-search mall-ai mall-resource mall-gateway
```

### Step 5 前端部署（ecs-user）
```bash
scp -r dist/* ecs-user@8.156.77.197:/data/csmall/frontend/dist/
```

### Step 6 索引清理（AI 可执行，见 §5）

### Step 7 验证（见 §6）

---

## 五、ES 索引操作（#33 清理）

```bash
# 前置：新 mall-ai 已上线（syncAll 逻辑已带状态校验+清理）
curl -X POST http://localhost:10010/ai/syncAll        # 重建 AI 索引 → 应 19 条（剔除下架 18）
curl -s http://localhost:9200/cool_shark_mall_ai/_count
curl -X DELETE http://localhost:9200/cool_shark_mall_index      # 删空索引
curl -X DELETE http://localhost:9200/cool_shark_mall_index2     # 删旧 search 索引（mall-search 已只读 AI 索引）
# 注意：删除索引前确认无 App 直连 /search（评估结论：Web 无消费者）
```

---

## 六、验证清单

### 6.1 基础设施
- [ ] 21 容器全 Up、无 unhealthy、RestartCount=0
- [ ] order 日志确认 Flyway V6 成功、oms_order 有 order_type 列
- [ ] Nacos 注册正常（尤其 mall-search 单实例、mall-product 9010）

### 6.2 第二批功能回归
- [ ] **#8** AI 预算：`ai:daily_cost:<北京时间日期>` key 用 Asia/Shanghai
- [ ] **#23** 注册传非法值 → 400（MethodArgumentNotValidException handler 生效）
- [ ] **#36** 订单队列 DLX：停消费者发消息 → 3 次重试后进死信队列
- [ ] **#14** 秒杀全链路：order_type=1 + 支付前校验 + 对账任务日志
- [ ] **#33** 搜索：
  - AI 搜索 `/ai/search` → 20→19 条（无下架 18）、有 aiExplanation
  - 普通搜索 `GET /search` → 同索引结果、图片 URL 完整（resource-host 生效）
  - **降级演练**：`docker stop csmall-ai` → 前端 fallback 秒回 + "AI 搜索暂不可用" → start 恢复
  - 商品增/删/下架 → ES 索引实时增减

### 6.3 安全回归（第一批不动摇）
- [ ] 登录/秒杀/支付全链路（JWT 新密钥下）
- [ ] Redis 带密码连通（R1~R4 后所有服务）

---

## 七、回滚

| 场景 | 回滚 |
|------|------|
| jar 有问题 | jars/backup-20260908/ 恢复旧 jar + `docker compose up -d --build <服务>` |
| Flyway V6 失败 | order 起不来 → 查日志；V6 是幂等 ADD COLUMN，可手动执行后重启 |
| 索引误删 | AI 索引随时 `/ai/syncAll` 重建；index2 删除前已备份文档清单 |
| compose 问题 | 备份的 compose 还原 + `up -d` |

---

**维护提示**: 本清单执行后，回填各 Step 实际结果到 [[TODO已完成]]（补"已上线"备注）并更新本文件状态为 ✅。
