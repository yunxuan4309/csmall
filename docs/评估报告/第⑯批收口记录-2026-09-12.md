# 第 ⑯ 批收口记录（简历前 · 2026-09-12）

> **本文件的定位**：本批「**简历前收口**」的**过程与决策记录**。两用 ——
> ① **修改记录**：做了什么、证据在哪、谁执行、什么状态；
> ② **提炼素材**：第三节列了"可能值得单独成篇"的认知点；**即使最后不提炼，本文件也作为修改记录保留**（不必归档）。
>
> **关联**：[[TODO文件]]（状态源 · 第一节"现在要做"）· [[TODO已完成]]（已完成明细）· [[文档索引]]（文档登记）· [[问题解决--代码与线上不一致的静默失效]]（本批多条认知与它同族）

---

## 一、本批范围（用户 2026-09-12 决策）

> **背景**：用户即将写简历，**不再扩展项目**，最多"修 bug + 录视频"。

| 分类 | 内容 |
|---|---|
| **做** | #55 SSH 暴露面（评估）· #57 Schema 漂移（只读核实）· #31 向量检索（可行性评估）· #54 RabbitMQ 凭据 · #61 外部端到端探活 · **#65 库存扣减 MQ 链路修复** · TODO 文档结构重构与瘦身 |
| **不做**（已标记暂不做） | #5-P1 热点参数限流 · #40-2 healthcheck · #22 CORS · #51 restart 策略 · #53 网关重试 · #45 Dubbo 应用名 · 企业级 7 项（#30/#39/#41/#15/#16/#26/#35） |
| **已接受**（不再挂账） | #56 公开仓库信息暴露（公开仓库正好当简历作品集）· **#55 的 `ecs-user` 密码登录**（密码强 + 学习用途） |

---

## 二、逐项交付与证据（**只有实测过的才写在"证据"列**）

| # | 结论 | 关键证据 | 落地文件 |
|---|---|---|---|
| **#65** 库存扣减 MQ 链路（P1 真缺陷） | ✅ 代码修复完成 · **待部署** | ① 根因：类级 `@RabbitListener`+`@RabbitHandler` 会**按载荷类型挑方法**，生产端发 `List<OrderItemMessage>` ⇒ `__TypeId__=java.util.ArrayList` ⇒ `String` 参数**挑不中**（日志 `No listener method found ... for class java.util.ArrayList`）② **把参数改成 `List<OrderItemMessage>` 也不够**（回归测试实测：`SmartMessageConverter.fromMessage(msg, methodParam)` **不按方法参数推断泛型**，元素退化成 `LinkedHashMap`）③ 改用**非泛型 POJO** `OrderStockMessage` 后回归测试 **3/3 绿 / BUILD SUCCESS** | `mall-order/.../mq/OrderStockMessage.java`（新）· `OrderQueueConsumer` · `OrderDlxConsumer` · `OmsOrderServiceImpl` · `OrderQueuePayloadContractTest`（新） |
| **#70** 🆕 库存扣减链路两个真缺陷（**#65 修复后暴露**） | ✅ 代码已修 · **待部署 mall-product + mall-order** | ① `SkuMapper.xml` `stock>#{stock}` ⇒ **买最后一件永远 0 行**（实测订单 `41528a60-…`：stock=1/quantity=1 ⇒ `1>1` false）② `basicNack(requeue=…)` 的 `countRequeue`（只数 `x-death`）在 **classic 队列恒 0** ⇒ **10 分钟重投 114 次**、DLX 恒空 ⇒ 改为 `AmqpRejectAndDontRequeueException` 抛异常进 DLX + 补 `application-prod.yml` 的 manual ack/限次重试 | `mall-product/.../mapper/SkuMapper.xml` · `mall-order/.../mq/OrderQueueConsumer.java` · `mall-order/.../application-prod.yml` · 🧩 **已提炼** → [[问题解决--消息可靠性与毒消息治理]] **§四 问题 3**（⚠️ 该节**证伪**了原 §3.2"用 x-death 做限次"的前提）|
| **#61** 外部端到端探活 | ✅ **已交付并已挂 cron**（本批由 AI 执行） | 新机 `/home/ai-deepseek/e2e_probe.py`（md5 `82cd96ff…` = 仓库版本）· cron 每 5 分钟 · 立即跑一次 **9/9 PASS** | `deploy/scripts/ops/e2e_probe.py` |
| **#57** Schema 漂移 | ✅ **关闭：0 处需要 ALTER** | 服务器 30 张业务表 × 快照 29 张；**列级 14 处差异全是 `ONLY_ON_SERVER`，`ONLY_IN_DDL`=0**；两处"疑似坑"追到根因 = `database/` 快照滞后（`seckill_message_retry` 是 Flyway `V5` 建的；`ams_permission.value` 是实体 `Permission.value` 声明过的字段） | 回填 4 份文档（TODO §57 · 三方漂移审计 · 10-6 · 静默失效） |
| **#31** 向量检索 | ✅ 可行性评估完成：**三项前置实测通过** | ① embedding 实测 HTTP 200 返回真实向量 ② ES 已有 `semanticVector: dense_vector(1024, cosine)` ③ `EmbeddingSelfCheck` 与运行时三分支降级**均已部署**（jar 时间 09-12 03:33 UTC > 修复提交）⇒ 改为 **env 覆盖、免重建镜像** | TODO §31 · `deploy/docker/docker-compose.yml`（新增 env 透传） |
| **#54** RabbitMQ 凭据 | ✅ 仓库侧已改 + 两步操作单 · **待执行** | 关键读码发现：`mall-order` 的 `application-prod.yml` **没有 rabbitmq 段** ⇒ 只认 `SPRING_RABBITMQ_*`（此前**永远**用默认 `guest/guest`）；而 `mall-seckill` 的 yml 用 `${RABBITMQ_USERNAME}` 占位符 ⇒ **两套变量都要保留** | `deploy/docker/docker-compose.yml`（3 个 MQ 服务各补 2 行）· TODO §54 |
| **#55** SSH 暴露面 | ✅ **已评估·接受并收口** | ① 两台 `passwd -S root` = **`root L`**、shadow = `*` ⇒ **root 锁定、无密码** ⇒ "公网可爆破 root"**不成立**（原结论是我照抄旧文档未验证严重度）② `ecs-user` 密码登录保留；实测老机 auth.log（09-06 起 6 天）**`Failed password` 247 次**（≈40/天）+ `Invalid user` 80 次 ③ 副产品：**密钥登录已打通**（`csmall_ecs_key` 公钥入两台 `authorized_keys`，指纹 `SHA256:UCv9+…` 与本地一致；落地证据 `/tmp/keyauth-evidence-OLD/NEW.txt`） | TODO §55（旧操作单已标 ⛔ 作废） |
| **TODO 文档** | ✅ 结构重构 + 瘦身 | **715 → 429 行（-40%）**：新结构 = 一、现在要做 → 二、暂不做 → 三、编号登记表 → 四/五/六 正文 → 七、历史摘要；**27 项关键串**逐项 grep 命中；CRLF 保持 | `docs/评估报告/TODO文件.md` |

---

## 三、方法论收获（**提炼候选** —— 每条都配了实测证据）

1. ⭐ **"测试自己也会说谎"，而且它会拦住你自己的修复**：#65 的第一版修复（参数改 `List<OrderItemMessage>`）是**被我自己写的回归测试否掉**的 —— 实测 `SmartMessageConverter` **不按方法参数推断泛型**，集合元素退化成 `LinkedHashMap`。⇒ 教训：**"看起来对"的类型契约，要用框架的真实转换器跑一遍**（不需要 broker 也能测）。
2. ⭐ **判据必须"对你真正要测的东西敏感"**：SSH 那轮我连错两次 ——
   - "sshd **PID 是否变化**" **不是** reload 的判据（sshd 是 `execve` 重执行，**PID 不变**）；
   - "客户端看到的 `Authentications that can continue` 列表" **不反映 `PermitRootLogin`**（它由 `PasswordAuthentication` 决定；root 账号**锁定**也照样列 `password`）⇒ 我据此得出的"root 仍允许密码"是**误报**。
   - ✅ 正确判据：`sshd -T -C user=…,host=…,addr=…`，或者**真去试一次**。
3. ⭐ **"照抄旧结论"= 把别人的判断当事实**：TODO §55 原写"公网可直接暴力破解"，实测 **root 本就是锁定账号** ⇒ 严重度不成立。⇒ 教训：**连"严重度"也要取证**（不只是"有无风险"）。
4. ⭐ **命令的交付方式也是工程的一部分**：`<KEY>` 这类**尖括号占位符**在 PowerShell 里直接报错（`<` 是保留运算符）；`[本地]` / `[服务器]` 的标注**必须做到不可误读** —— 本批两次把本地命令粘进服务器 shell（一次还静默写了个 0 行文件）。⇒ 改进：**把结果写到双方都能读的 `/tmp` 文件**（本批后半程改用这招后，终端关了也不影响取证）。
5. ⭐ **环境事实要先查，再动手**：Ubuntu 24.04 的单元名是 **`ssh`**（不是 `sshd`），且**默认 socket 激活** —— 改端口 / `Match` 需要 `systemctl daemon-reload` + `systemctl restart ssh.socket`（[Ubuntu bug 2069041](https://lists.ubuntu.com/archives/foundations-bugs/2024-June/517043.html)）；generator 处理不当会让 sshd 以**默认配置**运行（[bug 2076023](https://lists.ubuntu.com/archives/foundations-bugs/2024-August/520089.html)）。
6. **观测手段本身可能是错的（与既有文档同族）**：`e2e_probe.py` **刻意不查 `/actuator/health`** —— 2026-09-10 那次"21 容器全 Up、health 200、业务挂 24.5 小时"里，坏的是**组件之间那条路**。另实测两条本项目判据：**鉴权失败是 HTTP 200 + body `state=401`**（不是 401 状态码）、**所有业务 API 都需登录** ⇒ 只看状态码的监控会把"没登录"当"一切正常"。

> ✅ **已落地的提炼（2026-09-12）**：#70 的两个真缺陷已提炼为 [[问题解决--消息可靠性与毒消息治理]] **§四 问题 3**（现象两副面孔 / 根因表 / off-by-one 伴随缺陷 / 4 条可复用修复 / 面试话术），并在该节**显式更正**了原 §3.2 的设计前提。

**提炼建议**：第 2、3 条**建议并入** [[问题解决--代码与线上不一致的静默失效]]（它已有 §四"变更做了≠生效了"，本批的两条正好是"**判据不敏感 / 严重度未验证**"的新增面）；若想单独成篇，题目可用《**判据要对你真正要测的东西敏感 —— 三次误判复盘**》。第 1 条适合并入 [[问题解决--压测结论的可信性（先证伪干扰项与工具自身）]]（"工具自身会说谎"）。

---

## 四、遗留与未做（如实记录）

| 项 | 状态 | 说明 |
|---|---|---|
| #65 部署 `mall-order` | ✅ **已部署并验收（2026-09-12）** | 容器内 jar md5 == `ae32970c…` · **`No listener method found` 0 次** · 消费者收到**正确类型**的消息 · 你下的单成功触发扣减链路（由此**暴露 #70 的两个真缺陷**） |
| #70 部署 `mall-product` + `mall-order` | ⏳ **待用户执行** | jar 已由 AI 传到老机 `/tmp`；部署后**那条积压消息会自然成功**（stock 1→0）⇒ 循环结束 |
| #54 换 MQ 凭据 | ⏳ **待用户执行** | 需 `ecs-user` 改 `.env` + `rabbitmqctl` + recreate 3 个服务 + 删 `guest` |
| #31 开向量检索 | ⏳ **待用户拍板** | 会改变搜索排序 ⇒ **录像定稿前决定** |
| #67① 录像 | ⏳ 待用户 | 手册已就绪；**建议 #65 部署后再录** |
| 归档（薄索引 / A·B 册 / 手册） | ⏳ 待录像定论 | 触发条件已满足，但建议等录像有说法 |
| 遗留小文件 | —— | `/tmp/keyauth-evidence-*.txt`、`/tmp/sshkey-evidence.txt`、`/etc/ssh/sshd_config.bak-2026-09-12`、`bak2-2026-09-12`（均无害，可随时删） |

---

## 五、本批提交记录（`git log`，便于回溯）

本批共 **7 个提交**（`ahead` 从 31 → 37）：

| commit | 内容 |
|---|---|
| `ea56e7c` | docs(todo): #55 收口为「已评估·接受」（更正严重度 / 量化攻击量 / 作废旧操作单 / 记两条判据教训） |
| `81ffa9a` | docs(todo): 暂不做 / 已完成两节瘦身（631 → 429 行） |
| `19d216d` | docs(todo): TODO 结构重构（要做前置 / 不做后置）715 → 631 行 |
| `294295f` | chore(compose): mall-ai 增加 #31 向量检索开关的 env 透传（免重建） |
| `cece0da` | fix(order)+feat(ops): #65 库存扣减 MQ 链路修复 + #61 外部端到端探活 + #31/#54/#57 评估回填 |
| `b574ff0` | docs: 归纳「变更做了 ≠ 生效了」（问题解决扩充 + 面试版 10-11 + 博客 18）—— **本批前半段** |
| `c4f5da2` | docs: 全部收口后的最终同步（薄索引 / 补册2 / TODO）+ 脚本自查修 1 处 |

> 📌 **归档提示**：本文件是"批次过程记录"，**不参与**「方案文档」的归档触发条件；**不要归档**（它是修改记录，长期留在 `docs/评估报告/`）。
