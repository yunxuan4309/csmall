<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cooxiao.mall.product.mapper.CategoryAttributeTemplateMapper">

    <!-- int insert(CategoryAttributeTemplate categoryAttributeTemplate); -->
    <insert id="insert">
        INSERT INTO pms_category_attribute_template (id, category_id, attribute_template_id)
        VALUES (#{id}, #{categoryId}, #{attributeTemplateId})
    </insert>

    <!-- int countByCategoryId(Long categoryId); -->
    <select id="countByCategoryId" resultType="int">
        SELECT COUNT(*) FROM pms_category_attribute_template WHERE category_id=#{categoryId}
    </select>

</mapper>
