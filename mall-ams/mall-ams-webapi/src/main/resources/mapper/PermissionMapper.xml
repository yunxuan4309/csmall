<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cooxiao.mall.ams.mapper.PermissionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.cooxiao.mall.pojo.admin.model.Permission">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="code" property="code" />
        <result column="description" property="description" />
        <result column="sort" property="sort" />
        <result column="gmt_create" property="gmtCreate" />
        <result column="gmt_modified" property="gmtModified" />
    </resultMap>
    <select id="selectPermissionsByAdminId" resultMap="BaseResultMap">
        select ap.*
            from
            ams_permission ap
        inner join
            ams_role_permission arp
            on
            ap.id=arp.permission_id
        inner join
            ams_role ar
            on
            arp.role_id=ar.id
        inner join
            ams_admin_role aar
            on
            ar.id=aar.role_id
        inner join
            ams_admin aa
            on
            aa.id=aar.admin_id
        where
            aa.id=#{adminId}
    </select>
    <insert id="insertPermission">
        insert into ams_permission (
        name,
        code,
        sort,
        description)
        codes (
        #{name},
        #{code},
        #{sort},
        #{description})
    </insert>
    <select id="selectPermissions" resultType="com.cooxiao.mall.pojo.admin.vo.PermissionVO">
        select * from ams_permission
        <where>
            <if test="name!=null">
                name like concat("%",#{name},"%")
            </if>
            <if test="code!=null">
                and code like contcat("%",#{code},"%")
            </if>
        </where>
    </select>
    <update id="updatePermission">
        update ams_permission
        <set>
            <if test="name!=null">
                name=#{name},
            </if>
            <if test="code!=null">
                code=#{code},
            </if>
            <if test="description!=null">
                description=#{description},
            </if>
            <if test="sort!=null">
                sort=#{sort},
            </if>
        </set>
        where id=#{id}
    </update>
    <delete id="deletePermission">
        delete
            from
            ams_permission
        where
            id=#{id}
    </delete>
</mapper>
