# 秒杀购物车订单服务修改2

## 一、购物车删除失败（NOT_FOUND）

### 问题简述

用户添加购物车正常，但删除购物车和清空购物车均失败，后端抛出 `CoolSharkServiceException(responseCode=NOT_FOUND)`。

### 根因分析

排查过程中发现三个层面的问题：

#### 1. 实体类缺少 `@TableId` 注解

order、ums、seckill、product 模块的实体类 `id` 字段均缺少 `@TableId` 注解，导致 MyBatis-Plus 在执行 `delete`、`updateById` 等操作时无法正确识别主键字段。

**涉及文件（共22个）：**

- order 模块：`OmsCart.java`、`OmsOrder.java`、`OmsOrderItem.java`
- ums 模块：`User.java`、`UserDetail.java`、`UserLoginLog.java`、`DeliveryAddress.java`、`RewardPointLog.java`、`ChangePasswordLog.java`
- seckill 模块：`Success.java`、`SeckillSpu.java`、`SeckillSku.java`
- product 模块：`Spu.java`、`SpuDetail.java`、`Sku.java`、`SkuSpecification.java`、`Category.java`、`Brand.java`、`Picture.java`、`Album.java`、`Attribute.java`、`AttributeTemplate.java`、`AttributeTemplateValue.java`、`BrandCategory.java`、`CategoryAttributeTemplate.java`

**修复方式：** 为所有实体类添加 `@TableId(value = "id", type = IdType.ASSIGN_ID)` 注解及对应的 import 语句。

#### 2. 删除方法未验证用户归属

`OmsCartServiceImpl.removeCart()` 原实现使用 `deleteBatchIds` 直接按 ID 删除，未验证购物车商品是否属于当前登录用户，存在越权删除安全隐患。

**修复方式：** 改用 `LambdaQueryWrapper` 添加 `userId` 条件，只删除属于当前用户的购物车商品：

```java
LambdaQueryWrapper<OmsCart> wrapper = new LambdaQueryWrapper<>();
wrapper.in(OmsCart::getId, Arrays.asList(ids))
       .eq(OmsCart::getUserId, userId);
int rows = omsCartMapper.delete(wrapper);
```

#### 3. JavaScript Long 类型精度丢失（核心问题）

雪花算法生成的 ID（如 `2043340191977463809`）超出 JavaScript `Number` 类型的安全整数范围（最大 `9007199254740991`），导致前端接收后末尾数字丢失（变成 `2043340191977463800`），传回后端时无法匹配数据库记录。

**修复方式：** 在 `mall-common` 模块新建 `JacksonConfiguration.java`，全局将 `Long` 类型序列化为字符串返回给前端：

```java
@Configuration
public class JacksonConfiguration {
    @Bean
    public Jackson2ObjectMapperBuilderCustomizer jackson2ObjectMapperBuilderCustomizer() {
        return builder -> {
            builder.serializerByType(Long.class, ToStringSerializer.instance);
            builder.serializerByType(Long.TYPE, ToStringSerializer.instance);
        };
    }
}
```

同时在 `MallCommonConfiguration.java` 的 `@ComponentScan` 中添加 `com.cooxiao.mall.common.config` 包路径，确保配置被所有模块自动加载。

---

## 二、修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `mall-pojo/order/model/OmsCart.java` | 添加 `@TableId` 注解 |
| `mall-pojo/order/model/OmsOrder.java` | 添加 `@TableId` 注解 |
| `mall-pojo/order/model/OmsOrderItem.java` | 添加 `@TableId` 注解 |
| `mall-pojo/ums/model/User.java` | 添加 `@TableId` 注解 |
| `mall-pojo/ums/model/UserDetail.java` | 添加 `@TableId` 注解 |
| `mall-pojo/ums/model/UserLoginLog.java` | 添加 `@TableId` 注解 |
| `mall-pojo/ums/model/DeliveryAddress.java` | 添加 `@TableId` 注解 |
| `mall-pojo/ums/model/RewardPointLog.java` | 添加 `@TableId` 注解 |
| `mall-pojo/ums/model/ChangePasswordLog.java` | 添加 `@TableId` 注解 |
| `mall-pojo/seckill/model/Success.java` | 添加 `@TableId` 注解 |
| `mall-pojo/seckill/model/SeckillSpu.java` | 添加 `@TableId` 注解 |
| `mall-pojo/seckill/model/SeckillSku.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/Spu.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/SpuDetail.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/Sku.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/SkuSpecification.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/Category.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/Brand.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/Picture.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/Album.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/Attribute.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/AttributeTemplate.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/AttributeTemplateValue.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/BrandCategory.java` | 添加 `@TableId` 注解 |
| `mall-pojo/product/model/CategoryAttributeTemplate.java` | 添加 `@TableId` 注解 |
| `mall-order/webapi/.../OmsCartServiceImpl.java` | 重写 removeCart 方法，增加用户归属验证和日志 |
| `mall-order/webapi/.../OmsCartController.java` | 修复参数接收方式 |
| `mall-common/config/JacksonConfiguration.java` | 新建，Long 序列化为 String |
| `mall-common/config/MallCommonConfiguration.java` | 添加 config 包扫描 |

---

## 三、经验总结

1. **实体类主键注解**：使用 MyBatis-Plus 时，实体类必须添加 `@TableId` 注解标注主键，否则 delete/updateById 等操作无法正常执行。
2. **接口安全**：删除操作应验证数据归属，防止越权操作。
3. **Long 精度问题**：雪花算法生成的 ID 传给前端时，必须序列化为字符串，避免 JavaScript 数值精度丢失。这是分布式 ID 方案配合前后端分离架构的常见坑。
