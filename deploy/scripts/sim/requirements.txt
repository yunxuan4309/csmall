# #48 造数脚本依赖
# ⚠️ 本文件仅作"有 PyPI 外网的环境"的可复现清单；**本项目两台服务器都用不到它**：
#    实测新机/老机均 **无外网**（pypi.org / archive.ubuntu.com / 清华源全不通，
#    仅阿里云镜像可达），且 `ensurepip` 缺失 → `python3 -m venv` 建出来没有 pip。
# ✅ 本项目实际安装方式（免 venv、免 PEP 668、免外网）：
#    sudo apt-get install -y python3-pymysql      # 装 PyMySQL 1.0.2 到系统 dist-packages
#    # requests 2.31.0 系统已自带，无需安装
# 📌 备用（坚持钉死版本时，故意绕过 PEP 668）：
#    pip3 install --index-url https://mirrors.aliyun.com/pypi/simple/ \
#         --break-system-packages pymysql==1.1.1
# ℹ️ 脚本只用到 pymysql.connect 与 pymysql.cursors.DictCursor，1.0.2 / 1.1.1 两版一致。
# 详见 docs/评估报告/Python模拟数据与AI并发测试方案.md §〇.1 D6（2026-09-11 晚修订）
requests==2.31.0
PyMySQL==1.1.1
